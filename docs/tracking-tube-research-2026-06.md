# Flown-trajectory certified safety: deep-research synthesis (2026-06)

Workflow: tracking-tube-deep-research (16 agents, 5 lit angles + 5 designs + adversarial eval).
Recommendation = HCT-D (Hodograph-Conformal Tube, Dynamics-shaped, envelope-clamped).

I have enough grounding from the codebase to make the wiring concrete. The cert confirms: `R = r_obs+r_body+d_safe+q_conformal(+eps_track)`, deficit `b=rho^2-S` assembled before `g_seg_worst_deficit` subdivides, `v_eff/delta` time-growing-tube path, `n_axes` geometry selector, `certify_segments_above_plane` for the fly-over half, and the harness today flies point-mass (`R=HR+D_SAFE`, eps_track=0). Final recommendation below.

---

# FINAL RECOMMENDATION: the flown-trajectory certified safety layer

## 1. The recommended approach â "HCT-D" (Hodograph-Conformal Tube, Dynamics-shaped, envelope-clamped)

Do not pick one menu item. The correct design is a graft: **HCT's evaluation machinery and sup-normalized conformal core**, fed by **CFCC/META-CQR's reference-derivative features (corrected to the right physics)**, hardened by **three fixes every adversarial verdict converged on**, and with **RM-TEB's control theory demoted to a planner-side clamp and an OOD monitor rather than the guarantee**. Concretely, five coupled pieces:

**(A) Tube SHAPE from the plan's own hodograph â but keyed on acceleration/jerk/curvature, not peak speed.** For each committed Bernstein/B-spline segment, the velocity control points are `V_k = nÂ·(P_{k+1}âP_k)/dur` and accel `A_k = (nâ1)Â·(V_{k+1}âV_k)/dur`, exact finite differences of the position control points â no model. Define the per-segment shape
`g(t) = c0 + c1Â·âa_ref(t)â + c2Â·âjerk(t)â + c3Â·Îº(t)Â·âv_ref(t)âÂ² (+ c4Â·âv_ref(t)â)`.
The `c`'s are **fixed dimensionless relative weights** (a one-time pooled non-negative least-squares fit, units normalized by thrust-to-weight), **not the safety knob**. This is the single most important correction the verdicts force: quad tracking error is driven by **tilt-to-accelerate lag = lateral acceleration, jerk, and curvature**, NOT peak speed. The FLOWN-COVER verdict's killer point is that `v_peak` systematically *under-covers exactly the tight evasive turns where collisions concentrate* and over-covers straight cruise. The 2nd-difference hodograph gives `a_ref`/`jerk`/`Îº` for free and soundly (convex-hull bounds), so there is no excuse to use the wrong covariate.

**(B) Tube CONSTANT from data â a single dimensionless split-conformal multiplier on a sup-normalized, per-COMMIT score.** Per independent calibration unit `i`, score
`s_i = sup_{t in unit} âp_flown(t) â p_plan(t)â / g_i(t)`,
and `ÎºÌ = â(1âÎµ_track)(N+1)â`-th order statistic of `{s_i}`. Then for a fresh exchangeable unit, `P(s_{new} â¤ ÎºÌ) â¥ 1âÎµ_track`, and that event is **exactly** the simultaneous all-t statement `âÎ´(t)â â¤ ÎºÌÂ·g(t)` for every t in the unit. The sup-normalization is the load-bearing trick that converts marginal CP into the all-t band a continuous-time certificate requires, while one scalar per unit keeps within-unit temporal correlation irrelevant to exchangeability.

**The decisive fix here (kills FLOWN-COVER's soundness hole):** the calibration unit MUST be the **committed replan window / commit you actually certify**, not the whole episode. Scoring per-episode-max but deploying per-window means a slow window that inherits overshoot from a prior hard turn (high Î´, low aggressiveness) is certified with a tube the episode-level quantile never bounded â a genuine soundness gap, not conservatism. Use **per-commit windows as the exchangeable unit**, and recover finite-sample validity against window-to-window correlation by taking **one score per episode = the worst window's normalized residual** (block/Mondrian-clustered CP). That is the quantity `ÎºÌ` actually bounds and the quantity you deploy.

**(C) Evaluate the tube SOUNDLY per-cell inside the cert's existing de Casteljau recursion.** `ÎºÌÂ·g(t)` is non-polynomial (norms), so it cannot fold into the deg-2 `rho^2` poly path the obstacle-side `v_eff` tube uses. Instead extend `g_seg_worst_deficit` (line ~294) to carry the subdivided `V_k, A_k` hulls alongside `S`; on each de Casteljau cell compute `g_max(cell) = c0 + c1Â·Ãhi + c2Â·Ä´hi + c3Â·(ÎºÌvÂ²)hi` from the cell's outward-rounded hodograph hulls, set `R_eff(cell) = R_base + ÎºÌÂ·g_max(cell)`, and form the deficit `R_effÂ² â S` on that cell. Sound (constant per-cell `R_eff` upper-bounds the true `rho_track` on the cell) and tight (as the cert subdivides, `g_max(cell) â g(t)`). `ÎºÌ=0` is byte-identical to today, exactly like the verified `v_eff=0` fallback.

**(D) Close the saturation cliff with a planner-side trackable-envelope CLAMP + control-theoretic hard floor â this is non-optional.** The META-CQR and FLOWN-COVER verdicts identify the one hole that conformal *cannot* paper over: tilt/thrust **saturation is a deterministic regime cliff, not the tail of a smooth exchangeable distribution**. If calibration flights never saturated, no quantile covers a saturation event â and that is precisely the event causing the grazes. Two enforced preconditions convert "hope we sampled the worst physics" into a guarantee precondition:
  1. Feed a **thrust/tilt-margin constraint to EGO** (or post-filter committed plans) bounding `a_ref`, `jerk`, `tilt = a_ref/g` to a **verified-trackable envelope** where you have *both* dense calibration data *and* a control-theoretic (contraction/ISS/differential-flatness) Î´-bound. Refuse to certify (â HOLD) any committed window whose hodograph hull exits this envelope.
  2. Inside the envelope, set a **hard floor** `rho_track â¥ rho_floor(a_ref, jerk)` from the flatness/ISS bound, so even an under-sampled-but-in-envelope segment carries a physics-backed minimum. Conformal `ÎºÌÂ·g` sits *on top* of this floor and supplies the tightness; the floor supplies extrapolation safety.

**(E) ACI drift layer + inter-tick Lipschitz pad.** Wrap a single ACI/SAOCP recursion on the per-unit realized-coverage indicator to nudge `ÎºÌ` between flights, restoring long-run coverage under genuine distribution shift (wind, battery, payload, simâreal) with no exchangeability assumption. And because `p_flown` is only ever *observed at control ticks* (there is no model of the flown path â the CFCC verdict's correct catch), fold a one-line model-light inter-tick pad into `d_safe`: `d_safe â¥ d_safeâ° + v_flown_maxÂ·Ît_tick`. This is what makes the discrete-tick conformal score actually imply *continuous-time* flown clearance; without it the "for all t on p_flown" claim is literally false.

### Exact guarantee

For one drone, per committed window, per obstacle, over the trusted horizon:
The certificate **deterministically** proves (outward-rounded de Casteljau, no sampling) the geometric event
`G = { âp_plan(t) â c_pred(t)â â¥ r_obs + r_body + d_safe + [q_obs + v_effÂ·(t+Î)] + ÎºÌÂ·g(t),  ât â [0,t_hi] }`.
Two distribution-free stochastic events: `T = {âp_flown(t)âp_plan(t)â â¤ ÎºÌÂ·g(t) ât}` with `P(T) â¥ 1âÎµ_track` (sup-normalized split-CP on the per-commit score), and the existing obstacle event `O = {âc_predâc_trueâ â¤ q_obs+v_effÂ·(t+Î)}` with `P(O) â¥ 1âÎµ_obs`. On `Gâ©Tâ©O`, two triangle inequalities give `âp_flown(t)âc_true(t)â â¥ r_obs+r_body+d_safe > 0` for all t. By union bound:

> **P(flown collision with any certified obstacle, this window) â¤ Îµ_track + Îµ_obs.**

### Assumptions (honest, load-bearing first)
1. **Exchangeability of the deployment commit-window with the per-commit calibration units, *under the deployed closed-loop config* (cert + tube ON) and the deployment drone+regime.** This is the only assumption the safety guarantee rests on. It is *not* automatic â it requires the fixed-point calibration in Â§4.
2. **Plans stay inside the verified-trackable envelope** (enforced by the Â§D clamp; violations â HOLD, not silent miss). This is what makes assumption 1 hold in the saturation regime.
3. Existing obstacle-side exchangeability (`q_obs`), unchanged.
4. Inter-tick pad â¥ true inter-sample flown excursion (Lipschitz, from `v_flown_max`).
5. Cert arithmetic soundness (already guaranteed by outward rounding).

**Explicitly NOT assumed:** any quadrotor dynamics model is correct; any disturbance bound; that `g`'s shape is right (wrong shape only loosens the tube via a larger `ÎºÌ`, never voids coverage). Coverage is **per-commit and marginal over the commit distribution**; mission-level statements need an explicit Bonferroni/union over windowsÃobstacles (state it â see Â§3).

## 2. Why it beats the alternatives on all four axes

**General across drones.** The shape `g(Â·)` is common multirotor physics (differential flatness: mass/inertia/controller enter only as the scalar coefficients and `ÎºÌ`). A new drone needs **one scalar `ÎºÌ` re-quantiled from a short flight log** â no HJ PDE (FaSTrack), no SOS/CCM metric re-synthesis (RCCM, which *requires a dynamics model*, contradicting "data not model"). That is the lightest adaptation that still carries a finite-sample guarantee. Crucially, generality of the **guarantee** is real (re-calibrate); generality of **tightness** transfers only insofar as the shape fits â honest and stated, not hyped.

**Guaranteed.** Distribution-free, finite-sample `P(flown collision) â¤ Îµ_track+Îµ_obs`, composing by union with the obstacle layer you already ship â the symmetric twin of `q_conformal`. Unlike RM-TEB, there is **no blind-window probability leak**: RM-TEB's "deterministic, eps-preserving" claim is mathematically false given its own admitted one-observer-time-constant detection latency (`P â¤ Îµ_obs + P(latency-pierce)`), and its "reserve thrust for worst-case slew" patch is circular because worst-case slew is the very model quantity under attack. HCT-D's guarantee is honest because it never claims zero probability mass.

**Tight.** Per-cell hodograph evaluation collapses the tube toward `ÎºÌÂ·c0` on benign cruise cells and fattens it only on the high-accel/high-curvature go-around cells where the quad actually lags â and only on those cells, pointwise in time, not window-max. This is strictly tighter than a flat conformal tube (candidate a), and avoids the worst-case-over-conditions TEB (FaSTrack/RCCM) that collapses reach. The acceleration/curvature normalizer (vs `v_peak`) puts the keep-out budget where collisions are, not on straightaways.

**Deployable / sim2real.** The guarantee rests on flown-data exchangeability, **not** on a dynamics model, so a wrong quad model cannot silently void it â the failure mode that disqualifies FaSTrack/RCCM/ISSf-CBF as stand-alones. The honest residual risk is *distribution shift*, handled by (i) on-target fixed-point calibration, (ii) the ACI drift layer, and (iii) the trackable-envelope clamp keeping deployment inside the calibrated regime. RM-TEB's own author concedes its only sim2real-surviving layer is the conformal fallback â i.e. the heavy RCCM/L1/contraction-monitor stack is load-bearing for *tightness and diagnostics*, not for the *guarantee*. So we keep that machinery only where it earns its place: as the envelope clamp (D) and an optional OOD/contraction-shift detector that triggers recalibration/HOLD.

**Why not the pure-learning META-CQR:** its meta-net, FiLM, Neural-Fly basis, and domain randomization **never enter the guarantee** â coverage reduces to "recalibrate the scalar `E` per drone" = candidate (a). The meta-machinery is unguaranteed tightness decoration with a heavy IBP-over-a-net conservatism penalty that fattens worst exactly on aggressive segments. Keep reference-conditioning (it's the right idea); drop the meta-learning as a *headline guarantee* and treat it as an optional tightness module only if a simple hodograph `g` proves too loose across the fleet.

## 3. Honest limitations / when it fails

- **Sim-only calibration = a SIM guarantee.** The project today has only MetaUrban. A `ÎºÌ` calibrated in sim and deployed on real hardware violates exchangeability *exactly as much as any model-based method* â it just fails silently. Until real-drone logs exist, the bound must be **labeled "sim-validated P(collision) â¤ Îµ,"** not an unqualified hardware guarantee. This is the single most important anti-hype caveat.
- **Closed-loop / feedback-induced non-exchangeability.** The safety layer selects which plans fly, so the deployment plan distribution is endogenous. The fixed-point calibration (Â§4) matches *one* closed-loop distribution; if the scene/environment distribution shifts, ACI repairs only in long-run average. **ACI gives time-averaged, not per-window, coverage** â a sudden gust or a first-encounter aggressive maneuver under fresh shift can pierce before adaptation catches up. The split-CP base is the real per-window guarantee; ACI is a backstop, never the sole mechanism.
- **Mission-level multiplicity.** Per-window `Îµ` must Bonferroni to `Îµ/(WÂ·M)` over a full mission's windowsÃobstacles, which raises the quantile and re-fattens the tube at mission scale. Mitigate with a per-mission max-score formulation, but state the mission-level number honestly â do not advertise per-segment tightness while quoting per-window `Îµ`.
- **Isotropic `âÎ´â` is a reach tax in tight gaps.** Bounding the full norm rather than directional clearance toward the obstacle throws away harmless orthogonal error. Secondary fix (pure tightness, no soundness cost): use a toward-obstacle directional clearance score.
- **Saturation outside the envelope = HOLD, not go-around.** When the *fastest-safe* maneuver the product sells genuinely needs near-saturation flight, the envelope clamp will refuse to certify and fall back to HOLD. That is the correct, safe behavior, but it is a throughput cost concentrated on exactly the headline maneuver â and it must be measured in the A/B, not assumed away.
- **`ÎºÌ` is effectively a max over flights at small Îµ.** `Îµ_track=0.01` needs `Nâ¥99` calibration flights to even define the quantile; "a handful of flights" is honest only at `Îµ_trackâ0.1â0.2`. State `N` and `Îµ` together.

**Hype to strike from any writeup:** (i) "zero-shot cross-drone *guarantee*" â only the shape transfers, the guarantee needs per-drone target data; (ii) "meta-conformal / weighted-CP transfers with no new flights" â needs density ratios / fleet-exchangeability you cannot verify on a deliberately-heterogeneous fleet; (iii) "model-free â sim2real-robust" â survives model *error*, not distribution *shift*, and simâreal *is* a shift; (iv) RM-TEB "deterministic eps-preserving" â false given its blind window; (v) Neural-Fly/L1 "guarantee" â it's a UUB on tracking error, not `P(collision)`, useful only to *shrink* Î´ before calibration.

## 4. Staged implementation plan for our codebase

**Stage 0 â make the harness produce tracking error (prerequisite).** `metaurban/ego_goaround.py` flies point-mass today (`R=HR+D_SAFE`, `p_flown==p_plan`, zero residual â confirmed). Insert the MetaUrban quadrotor model between `ego.eval(te)` and the state update so `p_flown` deviates. Without this there is nothing to calibrate.

**Stage 1 â HARVEST** (`metaurban/track_harvest.py`, mirror `conformal_harvest.py`). Per committed window log: the committed Bernstein control points (already available via EGO `get_control_points` + `getInterval`), `p_flown` sampled densely at flown timestamps, the analytic `a_ref/jerk/Îº` from the hodograph, and the drone-param vector Ï. Exchangeable unit = **one per-commit window**, with **one score per episode = worst window** for block-CP. Run ~200 domain-randomized seeds (start/goal/crowd/speed) for a stable `Îµ_track=0.05` quantile.

**Stage 2 â CALIBRATE** (`metaurban/track_conformal.py`, mirror `conformal_calibrate.py`).
 - Stage-2a (shape, not safety-critical): non-negative LS of `âÎ´â` on `(âa_refâ,âjerkâ,ÎºvÂ²,âvâ)` â fixed `(c0,c1,c2,c3)`.
 - Stage-2b (the guarantee): sup-normalized split-CP on the per-commit score, block-by-episode for exchangeability, validated marginal coverage on held-out episodes, for `Îµ â {0.20,0.10,0.05}`.
 - Emit per-drone JSON `{Îµ, c0,c1,c2,c3, ÎºÌ[Îµ], v_calib_max, a_calib_max, envelope, N}`.
 - **Fixed-point loop (mandatory):** harvest under `ÎºÌâ`, recompute `ÎºÌâ`, re-harvest with the tube ON, iterate until `ÎºÌ` stabilizes â this repairs the closed-loop exchangeability break. Freeze the converged `ÎºÌ`.

**Stage 3 â CERT core** (`cpp/include/sando_cpp/bernstein_cert.hpp`). Add a `g_seg_worst_deficit_track` variant carrying subdivided `V_k,A_k` hulls and inflating `R_eff` per cell; add params `{ÎºÌ, c0..c3}` to `certify_segments_vs_sphere` and `certify_segments_above_plane` (the fly-over half), defaulting to 0. Add the `rho_floor` hard-floor term (Â§D). Add a ctest asserting `ÎºÌ=0` is **byte-identical** to today (regression guard, mirroring the `v_eff=0` invariant) and a grazing test (a flown overshoot that `p_plan`-only certifies must now be caught). **Rebuild both hand-built `.so` manually** (`sando_capi.so`; `ego/capi/ego_capi.so` with `-Wno-narrowing`) â they are outside CMake per CLAUDE.md.

**Stage 4 â STATIC GATE / RTA wiring.** In `ego_goaround.py` (and `ego_maneuver.py`): load the per-drone JSON; pass `{ÎºÌ,c0..c3}` through `certify/certify_horizontal(q_xy)/certify_above(q_z)`; add the inter-tick pad to `D_SAFE`; **enforce the trackable-envelope clamp** â if a committed window's hodograph hull exits `envelope`, refuse to certify â HOLD. Slot all of this into the existing gatekeeper-style certify-committed-or-HOLD loop. `eps_track` (the reserved additive slot documented at header line 12-13) is now filled by `ÎºÌÂ·g`.

**Stage 5 â ONLINE drift.** ACI/SAOCP wrapper (`metaurban/aci_track.py`) updating `ÎºÌ`'s multiplier from each flight's realized max normalized residual.

**Stage 6 â AUDIT / A/B.** Extend `ab_runner.py`/`ab_replay.py`: report reach/throughput and executed min-clearance with tube ON vs the current `eps_track=0`, flown through the real quad. **Headline number: close the 0.1â0.4 m static graze and restore M1's executed clearance from 0.677 m back above `d_safe=0.8`** without collapsing reach. Report empirical per-commit miss-rate â¤ `Îµ_track` and the HOLD-rate cost of the envelope clamp.

**Per-drone adaptation interface.** A single JSON `{Îµ, c0,c1,c2,c3, ÎºÌ[Îµ], v_calib_max, a_calib_max, envelope, N}`. The C++ cert is identical across drones; it is handed only these numbers. New drone = a short on-target flight campaign â refit `c`'s (optional) + re-quantile `ÎºÌ` (required) + measure the envelope. No PDE, no SOS, no metric, no recompile.

## 5. What is genuinely novel/publishable vs standard

**Standard (cite, don't claim):** split conformal (Vovk; Angelopoulos-Bates), normalized/CQR scores (Romano et al.), ACI/SAOCP (Gibbs-CandÃ¨s; Bhatnagar), the FaSTrack "planning-model vs tracking-model" framing, contraction/RCCM tube *form*, differential-flatness covariates (Mellinger-Kumar; Lee SE(3)).

**Genuinely novel â your contribution surface (no prior work found doing exactly this, corroborated across all five literature digests):**
1. **A planner-agnostic *continuous-time* Bernstein collision certificate whose flown-trajectory coverage comes from a conformally-calibrated, hodograph-shaped tracking tube evaluated *inside the certificate's own de Casteljau recursion*.** The fusion of a sound interval/convex-hull all-t certificate with a sup-normalized split-CP tube â and the per-cell hodograph evaluation that keeps it both sound *and* tight â is new. Existing conformal-tracking work (Lindemann/Dixit) is sampling-based MPC-side; existing Bernstein certs are model-side deterministic.
2. **The symmetric two-tube architecture:** the *same* split-CP machinery covers obstacle-prediction error (`q_conformal`) *and* ego tracking error (`ÎºÌÂ·g`), composing by union into one continuous-time certificate. Treating prediction error and tracking error as statistically symmetric distribution-free tubes added to the same Bernstein radius is a clean, novel framing.
3. **Control-theory-as-precondition, not as-guarantee:** using the flatness/ISS bound only to (a) pick the shape covariates and (b) define an *enforced trackable envelope* that makes the conformal exchangeability assumption hold in the saturation regime â converting the one thing conformal provably cannot do (extrapolate to an unsampled deterministic cliff) into a planner constraint. This is the principled resolution of the saturation-blindness critique and is, as far as the literature shows, unexplored.
4. **The honest negative results** are themselves publishable: that `v_peak` is the wrong normalizer (collisions live in low-speed high-curvature evasions), that pure control tubes' guarantees silently void under sim2real model error while conformal's silently void under distribution shift, and that the calibration unit must equal the deployment commit unit or the finite-sample guarantee degrades â these are precise, demonstrable claims with an A/B that isolates tracking error as the entire residual-collision gap.

**Not novel / do not overclaim as contributions:** the meta-learning/Neural-Fly transfer story (it doesn't enter the guarantee), zero-shot cross-drone (it isn't), and any "deterministic sim2real-robust tube" language (no method on the menu delivers it; ours is honestly probabilistic-and-model-free, which is the defensible thing to claim).