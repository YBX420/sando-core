{
  "summary": "Fundamental redesign: anticipatory planning + safety-band, smoothness from prediction not reaction",
  "agentCount": 6,
  "logs": [
    "[design:spacetime] failed: API Error: Server error mid-response. The response above may be incomplete.",
    "[design:field] failed: API Error: Server error mid-response. The response above may be incomplete."
  ],
  "result": "# GapWeave v2 最终蓝图 —— ETA-同步占据投影 + 净空带锦标赛(规划-认证信息对称化)\n\n骨架 = 幸存方案 GapWeave;两份 non-fatal 审查的全部修补(v1:F1-F6,v2:F1-F9)已逐条收编(映射表见 §13);另含 2 处合成期修正(§13 末),均已注明理由。本蓝图所有行号/常数/接口均已对照实盘代码核实(2026-07-07)。\n\n---\n\n## 0. 数字基线校正(先于一切——提案引文与实盘不符)\n\n实盘 `out/conformal/calib.json`(eps=0.05,冻结):**全类 q=1.054、v_eff=0.996**(不是提案引的 \"q≈0.5/v_eff≈1.6\")。`calib_v2.json`(FS3C-R,CALIB_V2=1 才生效):ped mature (0.9745, 3.1734)、young (q0y=1.338, growth=2.2);vehicle (1.2213, 3.1607)、young (1.392, 11.0);static (0.8542, 0);animal UNCALIBRATED(fail-closed)。冻结 benchmark = CALIB_V2=0 = v1 calib。\n\n由此,ped 证书半径 R = r_obs 0.30 + R_DRONE 0.25 + D_SAFE_H 0.10 + q 1.054 = **1.704 m**;管 ρ(t)=R+0.996·(t+0.30):ρ(0)=2.00,ρ(0.75)=2.75。`min_clr` 度量(replay_core L654,drone 视作点)的证书地板 = ρ(t*)−r_obs ∈ **[1.70, 2.00] m**(t*∈[0,DT])。\n\n**诚实推论(改写带的预期)**:实测 min_clr≈1.95 m 落在 [1.70,2.00] 内 ⇒ 最近拍已基本贴地板。过保守的大头不在\"最近拍多付净空\",而在:(a) 绕行弧线上非最近拍远超地板(直线性损失);(b) hold/evade/换挡的时间与姿态代价;(c) 管本身的 1.70。本方案交付 (a)(b);(c) 归另两条已立项线:proximity-recert(δ 0.3→0.1,按实盘 v_eff 只省 **≈0.20 m**,不是提案说的 0.3-0.5)+ q 瘦身(Mondrian/age-conditional)。绝对净空带 [0.5,1.0] 在冻结 calib 下**不可达**,带 = \"地板之上超额 M∈[0,0.5] m\"。塔菲大人的本意(多余净空换速度与直线性)在任何地板高度下成立;地板降下来时绝对带自动兑现。\n\n**安全措辞(定稿)**:认证拍逐拍 P≤ε 语义零改动;conformal 允许漏的尾部(ε=5%)之危害用 §11-2 尾部指标 + §3c 可逃逸钳显式监控管理。不说\"不会不安全\"。\n\n---\n\n## 1. 总体架构\n\n三层,全部发生在\"生成被认证参考\"之内,证书核/标定池/vendored EGO 零改动:\n\n1. **喂食层(ETA-带占据投影)**:EGO 的占据云从\"行人现在在哪\"换成\"无人机到达时行人在哪\",半径 = 届时证书会索要的半径(封顶 R_cap)。规划与证书看同一个未来 → 计划一次成型即过证,复发换挡链断根。\n2. **评分层(净空带)**:字典序(速度优先)不动;同速档进度**量化平手**时,超出地板 0.5 m 的裕度小者胜(把多余净空换成直线性);绑定 crosser 存在时插入 VO 切缝胡萝卜(pass-behind 优先)。\n3. **收尾层**:可逃逸速度钳(尾部危害缓冲)、A_SW 舒适门(非紧急切换推迟,上限 2 拍)、重建云协议(任何 replan 之后必飞\"最后一次 replan 且过证\"的样条,断言兜底)。\n\n---\n\n## 2. ① 规划器消费 KF 预测 —— `eta_footprints`(喂点接口内,EGO 本体零改动)\n\n### 2.1 采样与半径(每 mover)\n\n- t_cpa = clip(−(dp·dv)/|dv|², 0, REP_PLANHI),dv = KF 速度 − v_plan·gdir_feed;gdir_feed = v_d 归一(|v_d|>0.3)否则 goal 方向(沿用现 CPA_CLOUD L502-504 逻辑)。\n- **v_plan(治 v1W8/v2W9a)**:v_plan = 0.5·v_plan_prev + 0.5·(s_prev·max_vel),s_prev 取 `_stick[\"s_prev\"]`(SPEED_SLEW 已维护),无 1.0 下限,初值 max_vel。\n- 采样时刻 = {t_cpa−ETA_BAND, t_cpa, t_cpa+ETA_BAND} 去重(0.05 s 栅格)截断 [0, REP_PLANHI];**外加 t=0 体环**(半径=基线 `_plan_r(r_o)`=r_o,非胖半径——合成修正②,§13)。\n- **时间匹配半径(封顶,治 v2W1)**:\n  R_feed(t) = min( r_o + R_DRONE + D_SAFE_H + q + **K_VEFF**·v_eff·(t_eff+δ), **ETA_RCAP** ) + B_MID,\n  t_eff = min(t, DT) 当 ETA_TVIEW=1(v1F4\"下一拍视角\"变体,S5 扫),否则 t。\n  实盘数字:ped R_feed(0.75)=min(1.704+1.046, 2.0)+0.25 = **2.25 m**(未封顶原为 3.00)。\n- q/v_eff 查找 = **`_tube_lookup`,从 `build_cylinders` L72-95 抽出共享**(refactor 非复写,young/NIS_GATE/SIGV_GATE demotion 一并进来;治 v1W7)。\n\n### 2.2 降级规则(全部\"不劣于冻结基线\"设计)\n\n- **slow mover**(|v_KF|<ETA_SLOWV=0.5 m/s)、**young**(age<4)、**zombie**(NIS/SIGV demoted)、**not-ready**:退回冻结基线单环(当前位置,`_plan_r` 半径)。治 v2W1 徘徊者封走廊(RADIUS_CONSIST 教训:evade 64→87)与 v2W9b(young 4 m 盘反封出路;证书仍用 young 管,安全不变;birth 拍 hold 单列预算)。\n- **人群**(>ETA_MAXM=6 movers):每 mover 单采样 t_cpa + **B_MID=0**(治 v1W6 后半)。\n\n### 2.3 gap-free 环采样(治 v2W2)\n\n`_ring_gapfree(c, R, z_lo, z_hi)`:n_th = max(12, ceil(2πR/ETA_SPACING)),n_z = max(3, ceil((z_hi−z_lo)/ETA_SPACING)+1);ETA_SPACING=0.5(≤2·infl−res = 2·0.45−0.2 = 0.7,留裕量)。R=2.25 时 29×4≈116 点/环,≤3 环 ≈350 点/mover(基线 72)。**REP_OURS_INFL 保持 0.45 不动**(它兼管密封与静态体格;治 v2W7,静态侧 `_plan_r` 不含 R_DRONE、0.45 正垫着它)。点预算/耗时由 S0 探针管(超 30% 回归 → 降 n_th 密度,不降 infl;v2F9e)。\n\n### 2.4 CPA_CLOUD 退役(治 v2W8)\n\nS0 第一个 commit:ETA_FEED=1 时忽略 CPA_CLOUD 并打告警;冻结基线臂显式定义 = `CPA_CLOUD=0 + linspace(0,DT,2) + _plan_r`;写入 PROTOCOL.md。CPA_CLOUD 的\"mature-only 才预判\"教训(vehicle_spawn_accel 尸检,L492-497 注释)已由 §2.2 young 降级继承。\n\n---\n\n## 3. ② 安全带进代价(证书硬下界零改动)\n\n**(a) 喂食带中央**:R_feed 里的 +B_MID(0.25)让 EGO 无约束解自然坐在地板+0.25 附近——一次规划即过证,消灭\"规划贴 0.75、证书要 2.0\"的必然翻车。这是带的主杠杆。\n\n**(b) 评分:量化平手判据(合成修正①,替换提案的 W_EX 线性减法)**。提案的 `sc = progress − W_EX·max(0, m_min−0.5)` 有反例:远离人群的直线候选 margin 天然大 → 被罚成负分,输给贴人绕行——反目标(v1W7 已预警此判据风险)。定稿改为**平手裁决**:排序键 `(s_ok, round(progress/BAND_QT), −min(excess, 5.0))`,excess = max(0, m_min−BAND_W),m_min=+inf(无 mover/纯 over 过)时 excess=0。BAND_QT=0.15 m,BAND_W=0.5 m。语义:速度档优先(不动)→ 进度按 0.15 m 量化 → 真平手时超额净空小者胜。W_EX 参数废弃。\nmargin 来源:**姊妹函数**(治 v2W5)`cert_clear_margin` / `cert_clear_warp_margin`——同 cert_clear/warp 的调用结构,返回 (ok, m_min);warp 分支 margin **×s 换回世界尺度**;certify_above 过关的 cylinder 记 +inf;空序列返回 (True, +inf)。`_cert_at` 内部改用姊妹函数并把 m_min 缓存到闭包(_best_s 最后一次成功调用即 s_ok 的 margin,7 μs 级,零额外结构)。\n\n**(c) 可逃逸速度钳 ESC_GATE(治 v1W1 尾部危害)**:锦标赛返回 (kind,s) 后、SPEED_SLEW 重证代码路径内(safety_layer L491-506 已有该模式):\ngap = min over 绑定 mover( horiz(p_d,c0) − (R + v_eff·δ) );d_need(s) = s·max_vel·DT + (s·max_vel)²/(2·max_acc) + |v_mover|·(DT + s·max_vel/max_acc)。gap < d_need(s) → 逐格降档直到满足(crawl 档恒可行,只降速不禁飞),降档必经 warp 重证。语义:贴缝的档位与贴缝的距离耦合——5% 尾部事件发生时,evade 从可刹停距离起跳,补回基线 1.95 m 几何默默吸收的缓冲。只降速 ⇒ 恒 sound。\n\n**(d) 切缝胡萝卜 GAP_CARROT(治 v2W4 后的版本)**:绑定 crosser(mature、非 slow)存在时,DIRS 里 gap_b(pass-behind 优先)/gap_a 生效:胡萝卜 = 距 p_d 距离 L、指向 c_cpa ± v̂·(R_cpa+GAP_MID) 的世界系点。**DIRS 静态恒含 gap_b/gap_a**,binding 缺席时 `_gsub` 返回 None 走跳过路径(`DIRS.index(inc)` 永不 ValueError);incumbent=gap_* 只允许 carrot=\"frozen\"(复用现世界冻结航点机制,与 07-06 moving-carrot spiral 教训一致)。诚实预期:s=1 穿缝被 frozen-at-current 合取压制,收益主要落在 s=0.6-0.9(warp 证书为 predicted-only,合法);全速穿缝依赖 S7 证据,不提前承诺。\n\n---\n\n## 4. ③ hold 的根本消解\n\n**机制**:现 hold/evade 主因是\"规划穿即将被占空间 → 证书枪毙 → 整格重赛 → 同类计划再被杀\"的复发链(弯道图 19 拍 = 同一空间反复杀同一类计划)。喂食对称化后,规划器根本不再提出会被枪毙的计划;复发链断,残余被拒只剩:\n\n| cause | 判据(工具先行,S0 落地) | 处置 |\n|---|---|---|\n| young | 绑定集(t_cpa<TAU,降级行计 t_cpa=0)存在 age<4 track | 真惊讶,合法;单列 birth 预算 |\n| boxed | cert_verdict3 = refuted | 几何真堵;多被 crawl/CRET 降档吸收 |\n| budget | verdict3 = unknown | 接 adaptive 预算线 |\n| rebuild | 重建协议双败(§6) | 必须 ≈0 |\n| mismatch | 其余 | **喂食是否到位的直接探针**,必须 ≤2% |\n\n**定量论证**(估计,以 evade_cause 计数器实测校验):contested 走廊每 ep 进锥新生 track 1-3 个;冻结 v1 calib 无 young demotion,newborn 垃圾速度导致的 flicker 历史为 1-3 拍;CALIB_V2=1 时 young 盘(q0y 1.338+2.2·(t+δ) ≈ 4 m 级)只在正压走廊时逼 hold 且 age_min=4 拍封顶;速度格网+TAU_SPEED 吸收时序冲突为慢档。⇒ 预期 contested ep hold+evade **中位 0-1 拍、p90≤4**;弯道链 19 → **≤5**(只剩首次惊讶拍)。计数器进 `counts[\"evade_<cause>\"]`(replay_core L637 的 counts.get 模式直接兼容)。\n\n---\n\n## 5. ④ 过渡平滑 —— K_BLEND 砍掉,只留 A_SW 舒适门\n\nEGO `replan(p,v,a,goal)` 从当前状态(含加速度)起步,参考在切换瞬间本就 C2 连续;实测抖动来自决策层复发(本方案①治)。**2-3 拍胡萝卜混合(K_BLEND)砍掉**(v2F4 + 提案自疑 #4/交互未析),S4 数据若显示残余 >2°/s 再议。保留唯一机制:\n\n**A_SW 舒适门**(仅 dwell 升级探测分支,即非紧急切换):升级候选 replan+严格证书通过后,取 a_new(DT) = s_up²·ego.eval(s_up·DT)[2];‖a_new−a_d‖ > A_SW(0.9 = 0.5·max_acc·DT)且 defer 计数 <A_SW_DEFER_MAX(2)→ 本拍不接受升级,落回 incumbent 重试分支(该分支本就 replan+重证,**不过证则落全格锦标赛**——\"先在位者重认证、后 jerk 门\"的次序由现有控制流天然保证,治 v1W5);defer 达上限强制接受。**紧急切换(在位者证书失效 → 全格)永不经过 A_SW**——安全先于舒适。仅在存在有效 incumbent 时启用(治 v2W9d)。约 10 行。\n\n---\n\n## 6. 重建云协议与认证不变量(治 v1W4 / v2W3,红线级)\n\n胜者档位比建云档位**快超过 1 格**(升档迟滞:降档不重建,治 v1W8/F6)才触发,每拍 ≤1 次:\n\n```\n重建: cloud2,binding2 = eta_footprints(..., v_plan=s_win·max_vel)\n      ego.update_cloud(cloud2) → replan(胜者胡萝卜) → 重跑 _cert_at(s_win)   # 必须重证\n失败: ego.update_cloud(cloud_orig)  # 本拍原云(局部变量留存;grid 无历史帧,必须重喂)\n      → replan(胜者胡萝卜) → 重证\n再败: kind=\"evade\", cause=\"rebuild\"\n```\n\n**终拍断言(免费,7 μs)**:任何非 evade 拍,应用 set-point 前对当前 committed 样条按最终档位补一次 cert(warp/整速对应);失败按 evade 处理并计 `cert_final_fail`(应恒 0)。顺带堵一个**现存隐患**:safety_layer L433-434 的 restore-replan(胜者样条被循环 clobber 后重 replan)当前**未重证**,靠 poly-init 决定论撑着——EGO_WARMSTART=1 或 rebuild 上线后即是真洞;断言把它显式封死。飞出的每一段必须是\"最后一次 replan 且过证\"的那条。\n\n---\n\n## 7. 改动文件与喂点(全部已核实)\n\n| 文件 | 位置 | 改动 |\n|---|---|---|\n| `metaurban/safety_layer.py` | L55-105 | `build_cylinders` 的 demotion 段(L72-95)抽成 `_tube_lookup(cls,age,coast,nis,sigv,calib,calib_v2,age_min)`,原函数改调用(行为零变,ctest/冻结臂回归确认) |\n| 同上 | 新增 | `EtaBinding` dataclass(c0,v,t_cpa,R_cpa,age)、`eta_footprints(...)`、`_ring_gapfree(...)`、`gap_carrots(...)`、`cert_clear_margin`、`cert_clear_warp_margin` |\n| 同上 | L300-436 v2 核 | DIRS 静态加 gap_b/gap_a(L378);`_gsub` 支持 gaps dict、全格循环与 dwell 循环加 `if gs is None: continue`;`_cert_at` 接姊妹函数缓存 margin;排序键换 §3b 量化平手;dwell 分支加 A_SW defer;新 kwarg `gaps=None` |\n| 同上 | L478-507 wrapper | SPEED_SLEW 重证路径内并入 ESC_GATE 逐格降档+重证 |\n| `metaurban/replay_core.py` | L488-514 | ETA_FEED=1 分支:`SL.eta_footprints(percepts, p_d, gdir_feed, v_plan, calib, calib_v2, ...)` 替换 linspace 云;CPA_CLOUD 退役告警;metas 存拍内(evade_cause 用) |\n| 同上 | L547 后 | 重建协议(§6)+ 终拍断言;`maneuver_decide_v2(..., gaps=SL.gap_carrots(...))` |\n| 同上 | L607-637 | evade 分支打 cause(cert_verdict3 已在 L668 有调用模式);`counts[\"evade_<cause>\"]` |\n| 同上 | L666-682 | BAND_LOG=1 时 hist 加 `exc` 键(conflict tick:tick_clr − F_tick,F_tick = (R_i−r_i)+v_eff_i·δ,i=argmin 净空且绑定);EVADE_CAUSE/BAND_LOG 门控保默认 hist 形状冻结 |\n| 同上 | L271 | 不动(REP_OURS_INFL=0.45);新 env 常数区加 REP_PLANHI 等 |\n| `metaurban/render_3d_video.py` | L1144-1166(独立 PR,S4) | kf_movers `(_oid,c3,vel,r_obs,d_safe)` → percepts 适配层(cls 从 native_objects、age 从渲染端 KF 存量穿线);mover 环块换 `SL.eta_footprints(mode=...)`:mode=\"d435i\" 不加 t=0 环(深度已画当前面,保 L1156-1161 已验证语义)、mode=\"gt\" 加 t=0 体环;静态/OCC_MEM/cone 一行不动;EGO_PLANHI 默认 0.7→0.75 |\n| `metaurban/make_figures.py` | 新图 | conflict-tick excess 直方图 before/after |\n| `PROTOCOL.md` | S0 | CPA_CLOUD 退役决议 + 冻结基线臂定义 |\n| `ego_bridge.py` / vendored EGO / `cpp` | — | **零改动** |\n\n---\n\n## 8. 伪代码(修正定稿)\n\n```python\n# ==== safety_layer.py ====\n@dataclass\nclass EtaBinding: c0: np.ndarray; v: np.ndarray; t_cpa: float; R_cpa: float; age: int\n\ndef eta_footprints(percepts, p_d, gdir, v_plan, calib, calib_v2, plan_hi=0.75, band=0.25,\n                   b_mid=0.25, delta=0.30, dt=0.30, r_cap=2.0, k_veff=1.0, t_view=False,\n                   slow_v=0.5, max_m=6, spacing=0.5, mode=\"gt\"):\n    pts, binding, metas = [], None, []\n    if len(percepts) > max_m: b_mid = 0.0                       # 人群降级 (v1F4)\n    for (trk, det, r_o, h_o, cls) in percepts:\n        ready = trk is not None and getattr(trk, \"ready\", False)\n        c0, v, _ = trk.state() if ready else (np.r_[np.asarray(det,float)[:2],1.5], np.zeros(3), None)\n        age = int(getattr(trk, \"n\", 0)) if ready else 0\n        q, veff, demoted = _tube_lookup(cls, age, ..., calib, calib_v2)   # 共享 refactor (v1F5)\n        if (not ready) or demoted or age < 4 or np.hypot(*v[:2]) < slow_v:\n            pts += _ring_gapfree(c0[:2], _plan_r(r_o, cls), 0.3, h_o, spacing)   # 冻结基线单环,恒不劣 (v2F1/F9b)\n            metas.append((0.0, age)); continue\n        dp = c0[:2]-p_d[:2]; dv = v[:2]-v_plan*gdir; dvn = float(dv@dv)\n        t_cpa = float(np.clip(-(dp@dv)/dvn, 0.0, plan_hi)) if dvn > 1e-6 else 0.0\n        ts = sorted({round(x,2) for x in (max(0.0,t_cpa-band), t_cpa, min(plan_hi,t_cpa+band))})\n        if len(percepts) > max_m: ts = [t_cpa]\n        if mode == \"gt\": pts += _ring_gapfree(c0[:2], _plan_r(r_o, cls), 0.3, h_o, spacing)  # t=0 体环,基线半径(合成修正②)\n        R_cpa = None\n        for tt in ts:\n            te = min(tt, dt) if t_view else tt                  # ETA_TVIEW 变体 (v1F4)\n            R_t = min(r_o+R_DRONE+D_SAFE_H+q + k_veff*veff*(te+delta), r_cap) + b_mid   # 封顶 (v2F1)\n            if abs(tt-t_cpa) < 1e-9: R_cpa = R_t                # 在 t_cpa 处捕获 (v2F9c/v1F5)\n            pts += _ring_gapfree(c0[:2]+v[:2]*tt, R_t, 0.3, h_o, spacing)\n        metas.append((t_cpa, age))\n        if binding is None or t_cpa < binding.t_cpa:\n            binding = EtaBinding(np.asarray(c0[:2]), np.asarray(v[:2]), t_cpa, R_cpa, age)\n    return (np.asarray(pts,float) if pts else np.zeros((0,3))), binding, metas\n\ndef _ring_gapfree(c, R, z_lo, z_hi, spacing=0.5):               # v2F2\n    n_th = max(12, int(np.ceil(2*np.pi*R/spacing)))\n    n_z  = max(3,  int(np.ceil((z_hi-z_lo)/spacing))+1)\n    return [[c[0]+R*np.cos(a), c[1]+R*np.sin(a), z]\n            for a in np.linspace(0,2*np.pi,n_th,endpoint=False) for z in np.linspace(z_lo,z_hi,n_z)]\n\ndef gap_carrots(p_d, gdir, L, binding, cruise_z, gap_mid=0.35):\n    if binding is None or np.linalg.norm(binding.v) < 1e-3: return {}\n    c = binding.c0 + binding.v*binding.t_cpa; vhat = binding.v/np.linalg.norm(binding.v)\n    out = {}\n    for name, sgn in ((\"gap_b\",-1.0), (\"gap_a\",+1.0)):          # pass-behind 优先\n        d = c + sgn*vhat*(binding.R_cpa+gap_mid) - p_d[:2]; n = np.linalg.norm(d)\n        if n > 1e-6: out[name] = np.r_[p_d[:2]+d/n*L, cruise_z]\n    return out\n\ndef cert_clear_margin(ego, cyl, tau=TAU, delta=None):           # v2F5;warp 版 margin*s 回世界尺度\n    d = tau if delta is None else delta; m = float(\"inf\")\n    for (c0,vv,aa,R,zc,veff) in cyl:\n        hp,mp = ego.certify_horizontal(c0,R,vv,aa,t_hi=tau,v_eff=veff,delta=d)\n        hc,_  = ego.certify_horizontal(c0,R,(0,0,0),t_hi=tau,v_eff=veff,delta=d)\n        vo,_  = ego.certify_above(zc,t_hi=tau,v_eff_z=0.0,delta=d)\n        if hp and hc: m = min(m, mp)\n        elif not vo:  return False, 0.0\n    return True, m                                              # 空/纯over -> (True, inf)\n\n# ---- maneuver_decide_v2 核内(骨架不动,四处小改)----\nDIRS = (\"straight\",\"gap_b\",\"gap_a\",\"around_l\",\"around_r\",\"around_l2\",\"around_r2\",\"over\",\"climb\")  # 静态 (v2F4)\ndef _gsub(kind):\n    if kind in (\"gap_b\",\"gap_a\"): return (gaps or {}).get(kind)     # None -> 调用处 continue\n    ...\n# 全格循环与 dwell 升级循环开头:  gs = _gsub(dk);  if gs is None: continue\n# 评分(§3b 量化平手,替换 (s_ok, sc) 键):\nok, m_min = _cert_at_margin(s_ok)                                # 姊妹函数缓存\nexcess = 0.0 if m_min == float(\"inf\") else max(0.0, m_min - BAND_W)\nkey = (s_ok, round(sc/BAND_QT), -min(excess,5.0)) if BAND_SCORE else (s_ok, sc)\n# dwell 升级接受前(非紧急;仅 inc 存在;治 v1W5/v2W9d):\nrr = ego.eval(min(s_up*DT, dur)); a_new = s_up*s_up*np.asarray(rr[2])\nif A_SW>0 and np.linalg.norm(a_new-a_d)>A_SW and state.get(\"defer\",0)<A_SW_DEFER_MAX:\n    state[\"defer\"]=state.get(\"defer\",0)+1        # 不接受 -> 落到 incumbent 重试(replan+重证;不过证自动落全格=紧急旁路)\nelse: state[\"defer\"]=0; ...accept...\n\n# ==== replay_core.py L488-514 喂点 ====\nif ETA_FEED:\n    if os.environ.get(\"CPA_CLOUD\",\"0\")==\"1\": print(\"[eta] CPA_CLOUD ignored (retired under ETA_FEED)\")\n    sp = float(_stick.get(\"s_prev\",1.0)); _stick[\"v_plan\"]=0.5*_stick.get(\"v_plan\",max_vel)+0.5*sp*max_vel\n    gdir_feed = v_d[:2]/np.hypot(*v_d[:2]) if np.hypot(*v_d[:2])>0.3 else gdir\n    cloud, binding, metas = SL.eta_footprints(percepts,p_d,gdir_feed,_stick[\"v_plan\"],calib,calib_v2,\n        plan_hi=REP_PLANHI,band=ETA_BAND,b_mid=B_MID,delta=DELTA,dt=DT,r_cap=ETA_RCAP,\n        k_veff=K_VEFF,t_view=ETA_TVIEW,slow_v=ETA_SLOWV,max_m=ETA_MAXM,spacing=ETA_SPACING)\nelse:  ...冻结基线 (CPA_CLOUD=0 + linspace(0,DT,2) + _plan_r)...\nego.update_cloud(cloud, p_d)\n...\nkind,_v2s = SL.maneuver_decide_v2(..., gaps=(SL.gap_carrots(p_d,gdir,L,binding,CRUISE_Z,GAP_MID) if GAP_CARROT else None))\n# 重建协议(§6):升档差>1格 -> 重建->replan->重证;败->原云回退三连;再败-> kind=\"evade\",cause=\"rebuild\"\n# 终拍断言:非 evade 拍 cert 最终样条@最终档,fail -> evade + counts[\"cert_final_fail\"]\n# evade 分支:cause = \"young\" if any(t<TAU and a<4 for t,a in metas) else {\"refuted\":\"boxed\",\"unknown\":\"budget\"}.get(v3,\"mismatch\")\n```\n\n---\n\n## 9. 参数与 env 门控(全部默认关,冻结基线可复现)\n\n| env | 默认 | 语义 |\n|---|---|---|\n| ETA_FEED | 0 | 总开关:ETA-带喂食 |\n| REP_PLANHI | 0.75 | 喂食时域(对齐 TAU;现 headless 实际只 [0,0.3]) |\n| ETA_BAND | 0.25 | ±采样带 (s) |\n| ETA_RCAP | 2.0 | 喂食半径封顶 (m)(v2F1) |\n| K_VEFF | 1.0 | 喂食 v_eff·(t+δ) 项系数(**S5 主扫轴** {0,0.5,1.0}) |\n| ETA_TVIEW | 0 | 时间项用 min(t,DT)+δ(v1F4 变体) |\n| B_MID | 0.25 | 带中央放松 (m);>ETA_MAXM 自动 0 |\n| ETA_SLOWV / ETA_MAXM / ETA_SPACING | 0.5 / 6 / 0.5 | 降级阈值 / 人群阈值 / gap-free 间距 |\n| ETA_REBUILD | 1 | 升档重建(迟滞,≤1 次/拍,协议 §6) |\n| GAP_CARROT / GAP_MID | 0 / 0.35 | 切缝胡萝卜 |\n| BAND_SCORE / BAND_W / BAND_QT | 0 / 0.5 / 0.15 | 量化平手判据(W_EX 废弃) |\n| ESC_GATE | 0 | 可逃逸速度钳 |\n| A_SW / A_SW_DEFER_MAX | 0.9 / 2 | 舒适门(0=关);紧急旁路 |\n| EVADE_CAUSE / BAND_LOG | 1 / 0 | 纯观测计数器 / hist exc 键 |\n| **冻结不动** | — | REP_OURS_INFL=0.45、TAU=0.75、DELTA=0.30、REP_DSAFE=0.10、TAU_SPEED=1、SPEEDS_CRAWL=1、SPEED_SLEW=1、速度格 (1.0,0.6,0.3,0.15)、标定池、CERT_DUAL=1(见 S7) |\n\n---\n\n## 10. A/B 步骤(每步独立 commit;跑法 conda `metaurban` + PYTHONPATH=/media/boxuan/Data2/projects/metaurban + LD_PRELOAD 三件套;长扫用 bench_shard.py 分片+中途落盘——训练机 MCE 会崩)\n\n- **S0 工具先行(零行为变化)**:`_tube_lookup` refactor + margin 姊妹函数 + evade_cause + BAND_LOG exc/F_tick + 尾部事件标记(PERCEPT_HARVEST 行 resid>q_cls@dh=DT 的拍及后 2 拍收 min_clr)+ per-tick 耗时直方图(update_cloud/replan 分列)+ corridor-seal 探针(per-tick replan 失败率)+ restore-replan 后补证 + CPA_CLOUD 退役进 PROTOCOL.md。跑冻结基线拿**带仪表的基线快照**(excess 分布、cause 分布、尾部 p5)。\n- **S1** ETA_FEED=1,headless:6 seeds×6 eps×{PERCEPT=gt,realistic},PERCEPT_SEED≥3。门:碰撞 0、干净抵达不倒退、mismatch 占比下降、seal 探针 ≤基线、耗时 +<30%。\n- **S2** +GAP_CARROT=1。\n- **S3** +BAND_SCORE=1 +ESC_GATE=1 +A_SW=0.9:核对无用时回退、switch 计数不回退(v1F6)、带落率向靶移动。\n- **S4** 渲染端独立 PR(适配层+共享 eta_footprints+EGO_PLANHI 0.75)→ `ops/smooth_lab.sh` 两靶(A=seed7/X、B=seed12/CS)。\n- **S5** 敏感性(分片):K_VEFF{0,0.5,1.0}×ETA_TVIEW{0,1}(主轴)、ETA_RCAP{1.8,2.0,2.4}、ETA_BAND{0.15,0.25,0.4}、B_MID{0.15,0.25,0.35}、ETA_MAXM{4,6,8}、infl{0.4,0.45}(res=0.2 格化后真不同;0.2/0.25 剔除——死轴)。\n- **S6** 28 场景 benchmark + 失效包络重跑 + **GapWeave-ON 轨迹上重验证 conformal 覆盖(只验证,池冻结)**(v1F2,治访问分布漂移)+ held-out;CI 不劣才翻默认。\n- **S7(独立、证据门控,唯一碰证书语义的元素)** CERT_DUAL=0 消融。门槛(v1F2 加强版):覆盖报告必须在 **GapWeave-ON 的 held-out 轨迹**上采残差(d_drone 分布对齐部署),并按\"预测让路方向\"切片报**条件覆盖**(对抗自选偏差:frozen 合取兜着 KF 速度符号错/ID-swap)。拿不到 ⇒ CERT_DUAL=1 永久,收益明文封顶在 s=0.6-0.9。\n\n---\n\n## 11. 验收硬门(全部达标才翻默认)\n\n1. **碰撞恒 0**:全臂 min_clr>0 且 rta.violations==0;`cert_final_fail`==0;`evade_rebuild`≈0。\n2. **尾部条件**(v1F1):resid>q 事件拍(含后 2 拍)min_clr **p5(after) ≥ p5(before) − 0.2 m**;不达 → 回调 B_MID/ETA_RCAP/ESC 参数。\n3. **效率**:replay 面干净抵达 **≥64.3%** 不倒退(预期 +5~8pp,天花板 78.3%);用时中位不劣 >5%。\n4. **平滑**:smooth_lab 两靶 ours 倾角速率 RMS **≤130°/s**(现 135-178,native 122);switch 计数与 speed churn 同步报告、不劣化。\n5. **净空带**:conflict tick(∃绑定 t_cpa<TAU)excess = tick_clr − F_tick(F_tick=(R−r_obs)+v_eff·δ,冻结 calib ped ≈1.70 m):**≥60% ∈[0,0.5] m、p50≤0.35 m**,并出 before/after 直方图;若 S0 基线显示 excess 大头本就 <0.5(§0 推论),此门改报\"分布不右移 + 3/4 达标\"并把收益归口 3/4。\n6. **hold/evade**:contested ep 中位 **≤1 拍**、p90 **≤4**;mismatch **≤2%**;弯道图 hold 链 **19→≤5**;young/birth 拍单列预算(不计入 mismatch)。\n7. **运行探针**:per-tick replan 失败率 ≤基线;per-tick 耗时回归 <30%(超了降 n_th 密度,不动 infl)。\n\n---\n\n## 12. W→修补收编映射(简)\n\nv1W1→§3c+§11-2+§0 措辞|v1W2/W3→S6 重验证+S7 加强门槛|v1W4→§6 协议+终拍断言|v1W5→§5 次序/defer 上限|v1W6→K_VEFF 主轴+ETA_TVIEW+人群 B_MID=0|v1W7→dataclass/R@t_cpa/_tube_lookup 共享/CPA_CLOUD 退役|v1W8→v_plan EWMA+升档迟滞|v2W1→ETA_RCAP+K_VEFF+slow 降级+seal 探针|v2W2→gap-free 公式+infl 0.45 不动+S5 轴修正|v2W3→§6 回退三连+cause=rebuild|v2W4→DIRS 静态+None 跳过+frozen-only+K_BLEND 砍|v2W5→margin 姊妹函数(×s/+inf/空护栏)|v2W6→S4 独立 PR+mode 参数|v2W7→infl 不动|v2W8→PROTOCOL 退役决议|v2W9→档速 v_plan/young 降级+birth 预算/R@t_cpa/A_SW 仅 incumbent/耗时直方图。\n\n**合成期修正(2 处,超出审查清单)**:① BAND_SCORE 由 W_EX 线性减法改为量化平手判据(减法会惩罚天然远离人群的直线候选——反目标;v1W7 预警的风险根治);② ETA 喂食保留 t=0 体环但用**基线小半径**(胖环贴当前=RADIUS_CONSIST 已判死,不贴则规划穿\"人现在的位置\"制造 mismatch evade——小体环两头都躲开,且喂食恒为基线超集)。\n\n## 13. 残余诚实点\n\n绝对带 [0.5,1.0] 冻结 calib 下不可达(地板 1.70,§0);全速穿缝受 frozen 合取压制,S7 前收益封顶 s=0.6-0.9;hold 预期是估计、以 S0 计数器实测为准;净空数字改善可能小于叙事(§0 推论:最近拍已贴地板),平滑/hold/用时改善独立成立;t_cpa 假设无人机直线匀速,绕行候选 ETA 次优(±band+证书兜底,只损效率);渲染端适配层是真移植工程(S4 独立 PR,勿并入 S1-S3);ESC_GATE/A_SW 是舒适/尾部启发层,不是证书的一部分,文档里不得写成保证。",
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Design"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Kill"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "design:spacetime",
      "phaseIndex": 1,
      "phaseTitle": "Design",
      "agentId": "a8488f7c7ec581930",
      "model": "claude-fable-5",
      "state": "error",
      "startedAt": 1783456586346,
      "queuedAt": 1783456582483,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/media/boxuan/Data2/projects/sando_py/sando-core/metaurban/…",
      "promptPreview": "# 根本性重设计任务(无人机认证安全层,RA-L 级)\n\n## 塔菲大人的重定义(设计北极星,逐字)\n- \"安全是从 0.5-1 都可以接受,它可以有一个可牺牲的区间\" —— 安全=约束带(净空 0.5-1m 可接受),不是越大越好;现在系统给 1.95m = 过保守,多余的净空应该换成速度与平滑。\n- \"平滑是一个我怎么'利用我知道人物的运行逻辑'从而跑的更快的东西\" —— 平滑不是滤波出来的,是**预判**出来的:知道每个行人要去哪,就该高速从预测缝隙里优雅穿过,而不是撞上了再刹/绕/hold。\n\n## 系统现状(硬事实)\n- 架构:感知(45°锥+KF-CV 追踪,每 0.3s)→ per-class conformal 管 R(t)=q0+v_eff·t(λ-SHAPE-H 合法标定,ped q0≈0.5m v_eff≈1.6)→ EGO-Planner(B-spline,静态占据…",
      "lastProgressAt": 1783456736882,
      "error": "API Error: Server error mid-response. The response above may be incomplete.",
      "tokens": 35983,
      "toolCalls": 8,
      "durationMs": 150536
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "design:field",
      "phaseIndex": 1,
      "phaseTitle": "Design",
      "agentId": "aab0add938bbaa624",
      "model": "claude-fable-5",
      "state": "error",
      "startedAt": 1783456586146,
      "queuedAt": 1783456582483,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -n \"def predict\\|def state\\|self.n\\b\\|sigma_v\" /media/…",
      "promptPreview": "# 根本性重设计任务(无人机认证安全层,RA-L 级)\n\n## 塔菲大人的重定义(设计北极星,逐字)\n- \"安全是从 0.5-1 都可以接受,它可以有一个可牺牲的区间\" —— 安全=约束带(净空 0.5-1m 可接受),不是越大越好;现在系统给 1.95m = 过保守,多余的净空应该换成速度与平滑。\n- \"平滑是一个我怎么'利用我知道人物的运行逻辑'从而跑的更快的东西\" —— 平滑不是滤波出来的,是**预判**出来的:知道每个行人要去哪,就该高速从预测缝隙里优雅穿过,而不是撞上了再刹/绕/hold。\n\n## 系统现状(硬事实)\n- 架构:感知(45°锥+KF-CV 追踪,每 0.3s)→ per-class conformal 管 R(t)=q0+v_eff·t(λ-SHAPE-H 合法标定,ped q0≈0.5m v_eff≈1.6)→ EGO-Planner(B-spline,静态占据…",
      "lastProgressAt": 1783456999229,
      "error": "API Error: Server error mid-response. The response above may be incomplete.",
      "tokens": 82276,
      "toolCalls": 12,
      "durationMs": 413083
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "design:open",
      "phaseIndex": 1,
      "phaseTitle": "Design",
      "agentId": "a3a6166b0b417b577",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1783456585023,
      "queuedAt": 1783456582483,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "【验收硬门(全部达标才翻默认)】 1. 碰撞恒 0:全部 A/B 臂 min_…",
      "promptPreview": "# 根本性重设计任务(无人机认证安全层,RA-L 级)\n\n## 塔菲大人的重定义(设计北极星,逐字)\n- \"安全是从 0.5-1 都可以接受,它可以有一个可牺牲的区间\" —— 安全=约束带(净空 0.5-1m 可接受),不是越大越好;现在系统给 1.95m = 过保守,多余的净空应该换成速度与平滑。\n- \"平滑是一个我怎么'利用我知道人物的运行逻辑'从而跑的更快的东西\" —— 平滑不是滤波出来的,是**预判**出来的:知道每个行人要去哪,就该高速从预测缝隙里优雅穿过,而不是撞上了再刹/绕/hold。\n\n## 系统现状(硬事实)\n- 架构:感知(45°锥+KF-CV 追踪,每 0.3s)→ per-class conformal 管 R(t)=q0+v_eff·t(λ-SHAPE-H 合法标定,ped q0≈0.5m v_eff≈1.6)→ EGO-Planner(B-spline,静态占据…",
      "lastProgressAt": 1783457103214,
      "tokens": 99100,
      "toolCalls": 26,
      "durationMs": 518191,
      "resultPreview": "{\"acceptance\":\"【验收硬门(全部达标才翻默认)】\\n1. 碰撞恒 0:全部 A/B 臂 min_clr>0 且 rta.violations==0(replay_core 已有 RTA 计数,certified tick 后同窗违约必须 0)。\\n2. replay 面干净抵达 ≥64.3%(不得倒退;预期 +5~8pp,天花板 78.3%)。跑法:6 seeds × 6 eps × {PERCEPT=gt, realistic} × {DECIDE=v2 基线, +ETA_FEED, +GAP_CARROT, +BAND_SCORE},PERCEPT_SEED 重采样 ≥3。\\n3. smooth_lab 两靶(ops/smooth_lab.sh,A=seed7/X、B=seed12/CS):ours 倾角速率 RMS ≤130°/s(现 135-178,native 12…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "kill:open",
      "phaseIndex": 2,
      "phaseTitle": "Kill",
      "agentId": "a55679285168077af",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1783457113530,
      "queuedAt": 1783457105317,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "【W1|最重|miscoverage-tail 危害放大 + 未认证 evade 近距化(…",
      "promptPreview": "# 根本性重设计任务(无人机认证安全层,RA-L 级)\n\n## 塔菲大人的重定义(设计北极星,逐字)\n- \"安全是从 0.5-1 都可以接受,它可以有一个可牺牲的区间\" —— 安全=约束带(净空 0.5-1m 可接受),不是越大越好;现在系统给 1.95m = 过保守,多余的净空应该换成速度与平滑。\n- \"平滑是一个我怎么'利用我知道人物的运行逻辑'从而跑的更快的东西\" —— 平滑不是滤波出来的,是**预判**出来的:知道每个行人要去哪,就该高速从预测缝隙里优雅穿过,而不是撞上了再刹/绕/hold。\n\n## 系统现状(硬事实)\n- 架构:感知(45°锥+KF-CV 追踪,每 0.3s)→ per-class conformal 管 R(t)=q0+v_eff·t(λ-SHAPE-H 合法标定,ped q0≈0.5m v_eff≈1.6)→ EGO-Planner(B-spline,静态占据…",
      "lastProgressAt": 1783457466600,
      "tokens": 55532,
      "toolCalls": 8,
      "durationMs": 353069,
      "resultPreview": "{\"fatal\":false,\"wounds\":\"【W1|最重|miscoverage-tail 危害放大 + 未认证 evade 近距化(具体反例)】方案的防御链是\\\"证书不动+fail-closed\\\",但 evade 本身是**未认证飞行**(safety_layer.py L156-159:'evade'=nothing certified→caller flees),而方案的既定目标就是把执行净空从 1.95m 压到地板+0.2(≈1.3-1.6m 表面)。反例代入数字:crosser 以 KF v=(0,1.6) 横穿,GapWeave 的 pass-behind 胡萝卜让无人机以 s=0.9(≈1.8m/s)在净空 1.35m 处贴缝;pass 拍行人发生 5% 尾部事件(急停反折,残差>q 正是 conformal 允许漏的那 5%),新预测管压住全部方向×速度格,verd…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "kill:open",
      "phaseIndex": 2,
      "phaseTitle": "Kill",
      "agentId": "a28d340b8547ed9b8",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1783457114946,
      "queuedAt": 1783457105317,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "【W1|封走廊:半径数值把喂食推回已记录两次的失败区】R(t)=r+R_…",
      "promptPreview": "# 根本性重设计任务(无人机认证安全层,RA-L 级)\n\n## 塔菲大人的重定义(设计北极星,逐字)\n- \"安全是从 0.5-1 都可以接受,它可以有一个可牺牲的区间\" —— 安全=约束带(净空 0.5-1m 可接受),不是越大越好;现在系统给 1.95m = 过保守,多余的净空应该换成速度与平滑。\n- \"平滑是一个我怎么'利用我知道人物的运行逻辑'从而跑的更快的东西\" —— 平滑不是滤波出来的,是**预判**出来的:知道每个行人要去哪,就该高速从预测缝隙里优雅穿过,而不是撞上了再刹/绕/hold。\n\n## 系统现状(硬事实)\n- 架构:感知(45°锥+KF-CV 追踪,每 0.3s)→ per-class conformal 管 R(t)=q0+v_eff·t(λ-SHAPE-H 合法标定,ped q0≈0.5m v_eff≈1.6)→ EGO-Planner(B-spline,静态占据…",
      "lastProgressAt": 1783457610574,
      "tokens": 64029,
      "toolCalls": 19,
      "durationMs": 495628,
      "resultPreview": "{\"fatal\":false,\"wounds\":\"【W1|封走廊:半径数值把喂食推回已记录两次的失败区】R(t)=r+R_DRONE+D_SAFE_H+q+v_eff(t+δ)+B_MID,冻结 calib ped q=1.054、v_eff≈1.6 代入:R(0)=2.43m、R(0.75)=3.63m,三采样(±0.25s)叠加后单个 ped 的有效 keep-out 直径≈7m。反例:6m 走廊、前方 3m 的徘徊 ped(v≈0.2m/s)——ETA 环退化到当前位置,等价于 replay_core.py L77 注释里已判死的 RADIUS_CONSIST 实验(q=1.054 封走廊,evade 64→87、time+12s)再加厚 1.0-1.9m;\\\"尾迹自动空出\\\"只在 mover 快时成立,v→0 时严格更差。渲染端 render_3d_video.py L1160-1…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "synthesize",
      "phaseIndex": 3,
      "phaseTitle": "Synthesize",
      "agentId": "ab2eb1790d9edd9ae",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1783457615724,
      "queuedAt": 1783457612002,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -n \"gdir\\|_stick\\|s_prev\\|REP_PLANHI\" /media/boxuan/Da…",
      "promptPreview": "# 根本性重设计任务(无人机认证安全层,RA-L 级)\n\n## 塔菲大人的重定义(设计北极星,逐字)\n- \"安全是从 0.5-1 都可以接受,它可以有一个可牺牲的区间\" —— 安全=约束带(净空 0.5-1m 可接受),不是越大越好;现在系统给 1.95m = 过保守,多余的净空应该换成速度与平滑。\n- \"平滑是一个我怎么'利用我知道人物的运行逻辑'从而跑的更快的东西\" —— 平滑不是滤波出来的,是**预判**出来的:知道每个行人要去哪,就该高速从预测缝隙里优雅穿过,而不是撞上了再刹/绕/hold。\n\n## 系统现状(硬事实)\n- 架构:感知(45°锥+KF-CV 追踪,每 0.3s)→ per-class conformal 管 R(t)=q0+v_eff·t(λ-SHAPE-H 合法标定,ped q0≈0.5m v_eff≈1.6)→ EGO-Planner(B-spline,静态占据…",
      "lastProgressAt": 1783458458879,
      "tokens": 108355,
      "toolCalls": 15,
      "durationMs": 843155,
      "resultPreview": "# GapWeave v2 最终蓝图 —— ETA-同步占据投影 + 净空带锦标赛(规划-认证信息对称化)\n\n骨架 = 幸存方案 GapWeave;两份 non-fatal 审查的全部修补(v1:F1-F6,v2:F1-F9)已逐条收编(映射表见 §13);另含 2 处合成期修正(§13 末),均已注明理由。本蓝图所有行号/常数/接口均已对照实盘代码核实(2026-07-07)。\n\n---\n\n## 0. 数字基线校正(先于一切——提案引文与实盘不符)\n\n实盘 `out/conformal/calib.json`(eps=0.05,冻结):**全类 q=1.054、v_eff=0.996**(不是提案引的 \"q≈0.5/v_eff≈1.6\")。`calib_v2.json`(FS3C-R,CALIB_V2=1 才生效):ped mature (0.9745, 3.1734)、young (q…"
    }
  ],
  "totalTokens": 445275,
  "totalToolCalls": 88
}