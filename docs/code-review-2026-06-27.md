# Code review (2026-06-27) — 安全层 + 公平基准的多维审查

> 26/34 个发现经对抗式验证确认为真(读真实代码+重跑数据)。6 维度并行 + 逐发现验证。

## [CRITICAL] delta_track=0.29 m is a unit-switch artifact; the honest flight-level conformal margin is 2.91 m
- **维度**: HCT-D TRACKING TUBE + CONFORMAL CORRECTNESS
- **位置**: `metaurban/track_conformal.py:98-122; metaurban/render_3d_video.py:798-804`
- **问题**: The deployed tracking margin is computed as the (1-eps) quantile over per-commit WINDOWS (n_windows_cal=4647), but windows within an episode are strongly autocorrelated (same quad, same maneuver), so they are NOT exchangeable draws. The exchangeable unit for a per-flight safety guarantee is the EPISODE (n=99). Computing the same eps=0.01 split-conformal quantile with the EPISODE as the unit yields delta_track=2.91 m (eps=0.05 -> 1.76 m, eps=0.10 -> 1.04 m), i.e. ~10x the deployed 0.29 m. The 0.29 m number exists only because the per-window pool inflates the effective n by ~100x.
- **影响**: The keep-out radius cert_R = r_obs+MAN_DSAFE+MAN_QCONF+MAN_TRACK is built on a margin ~10x too small for the advertised 99% flight-level coverage. The certificate's guarantee that the FLOWN path clears the obstacle is therefore false at the claimed confidence.
- **修法**: Calibrate delta_track on the episode-level max score (one exchangeable unit per flight) or use a hierarchical/cluster-robust conformal correction using the number of episodes, not windows. Report the resulting (much larger) margin honestly, or restrict the claim to per-window marginal coverage and state the per-flight breach rate.
- **验证**: VERIFIED by reading code and re-running on the 99 actual harvest .npy files. track_conformal.py:98-104 window_maxes() flattens every commit window from all cal episodes into one pool; :116-118 computes delta_track as the (1-eps) split-conformal quantile of that per-WINDOW-max pool. track_calib.json confirms eps=0.01 -> delta_track_m=0.29067, n_windows_cal=4647, n_episodes=99. render_3d_video.py:798 MAN_TRACK default 0.29; :936 R=r_obs+MAN_DSAFE+MAN_QCONF+MAN_TRACK.

Empirical reproduction matches the finding EXACTLY: deployed per-window eps=0.01=0.2907 m; recomputed with the EPISODE as the exc

## [CRITICAL] 30% of flights breach the 0.29 m tracking margin; +0.29 does not bound the flown path
- **维度**: HCT-D TRACKING TUBE + CONFORMAL CORRECTNESS
- **位置**: `metaurban/render_3d_video.py:936-940 (cert_R, z_clear); 1456-1459 (harvest)`
- **问题**: The certificate certifies the PLANNED B-spline and adds a constant 0.29 m for plan->flown deviation, relying on triangle inequality ||flown-obs|| >= cert_R - ||flown-planned||. But ||flown-planned|| exceeds 0.29 m in 30/99 episodes (per-episode max deviation up to 2.91 m), so on ~1/3 of flights there is at least one committed window where the flown drone is up to ~2.6 m INSIDE the certified keep-out.
- **影响**: The central product claim ('cert_R bounds what FLIES, not just what was planned', render_3d_video.py:801-802) is empirically false for a third of flights. A continuous-time collision certificate that the flown path violates on 30% of runs is not a certificate.
- **修法**: Either (a) size delta_track to the flight-level conformal margin so per-flight breach <= eps, or (b) explicitly down-rank the tracking margin from a 'guarantee' to a 'typical-case' margin and report the empirical per-flight breach rate (30% at 0.29 m). Pair with an enforced replan/hysteresis precondition that actually bounds the tail.
- **验证**: CONFIRMED on every checkable claim.

CODE: The dynamic-mover certificate is render_3d_video.py:934-943 (cert_clear). Line 936: `R = r_obs + MAN_DSAFE + MAN_QCONF + MAN_TRACK   # +tracking margin so the cert covers the FLOWN path`, and line 939 adds MAN_TRACK to certify_above's z_clear. MAN_TRACK defaults to 0.29 m (line 798). The cert calls `ego.certify_horizontal` on the PLANNED B-spline; the only accounting for plan->flown deviation is the constant +0.29. flown_samples() (the actual forward-sim of the real quad, line 945-962) is used ONLY for STATIC clearance (static_clear, line 967), NOT fo

## [CRITICAL] Maneuver flies an UNCERTIFIED climb fallback (no cert, no HOLD) and can collide while claiming 'certified'
- **维度**: CERTIFICATE SOUNDNESS AS USED
- **位置**: `metaurban/render_3d_video.py:984-993 (climb commit), :1334-1338 + :1389-1403 (no HOLD path), :1435-1436 (always flies)`
- **问题**: In the fastest-safe tournament, every real candidate (straight/around x4/over) must pass cert_clear() AND static_clear(). But the terminal fallback commits 'climb' with NO certificate check: `ego.replan(p_d, v_d, a_d, np.array([p_d[0], p_d[1], z_top])); chosen = "climb"`. This branch is reached precisely when NO candidate certified (i.e. a mover is uncleared / the drone is boxed in) — the most dangerous case. In --maneuver mode there is no HOLD: the main loop only computes a speed scale (EGO_FOVCAP) and always flies whatever EGO holds (ego_cert_hold is never set in the maneuver branch; line 1435 evaluates s because not ego_cert_hold). A straight-up climb keeps the drone at its current (x,y); if a mover is at/near that (x,y) — the boxed case — the drone climbs straight through the mover's height band uncertified. The disjunction's vertical proof (vo) also fails at t=0 of such a climb (p_z starts at CRUISE_Z=1.5 < z_clear~3.0), so even 'over' typically can't certify against a near mover and falls through to this uncertified climb.
- **影响**: Breaks the central product claim. The 'certified fastest-safe go-around' can fly an unproven trajectory exactly when a certificate could not be found, producing a real collision the headline says is impossible. This is the seed-56 climb-into-a-tall-mover hole: it is NOT handled in maneuver mode.
- **修法**: Gate the climb like every other candidate: only commit climb if cert_clear() (and static_clear()) pass; if even the climb cannot certify, fall back to a genuine HOLD/decelerate-to-hover (set ego_cert_hold=True) rather than flying it. At minimum, run cert_clear() on the committed climb and force hover when it fails.
- **验证**: Confirmed by reading render_3d_video.py and ego_bridge.py. In ego_maneuver_replan the straight/around candidates (L982) and the 'over' candidate (L985-987) are committed only behind `cert_clear() and static_clear()`, but the terminal climb at L989 is unconditional: `ego.replan(p_d, v_d, a_d, np.array([p_d[0], p_d[1], z_top])); chosen = "climb"` — no cert guard, reached as the else of the over branch i.e. exactly when NOTHING certified (boxed). The climb target keeps the current (x,y) and rises to z_top, i.e. a vertical pass through the mover's column. In --maneuver mode there is no HOLD: `grep

## [HIGH] Conformal tube calibration cadence/noise != deployment — the (1-eps) coverage guarantee does not transfer
- **维度**: CERTIFICATE SOUNDNESS AS USED
- **位置**: `metaurban/conformal_calibrate.py:35-38 + out/conformal/calib.json (dt_ctrl=0.3, meas_noise=0.07, delta=0.3); metaurban/render_3d_video.py:655, :831, :937`
- **问题**: The conformal keep-out (q_conformal=0.125, v_eff=0.61) was calibrated by running the KF at DT_CTRL=0.30, MEAS=0.07, DELTA=0.30. The calibration file's own docstring asserts these 'must match scenario.py + kf_tracker defaults so calibration == deployment'. But deployment runs the KF at dt=REPLAN_DT=0.10 and meas_noise=KF_MEAS_NOISE=0.10, and the cert uses delta=REPLAN_DT=0.10. Higher deploy measurement noise (0.10 vs 0.07) inflates the real KF residual beyond the calibrated tube, and the dt change alters KF gain/lag — both push true coverage below the claimed 1-eps. MoverTracker's own defaults (dt=0.30, meas_noise=0.07) match calibration, but render_3d_video explicitly overrides them.
- **影响**: The probabilistic safety claim 'P(collision) <= eps via conformal keep-out' is invalid at deploy: the tube was fitted to an easier (lower-noise, longer-step) residual distribution than the one actually generated, so realized coverage is below target. This is also an experimental-fairness issue if reported coverage is taken from calib.json.
- **修法**: Either re-run conformal_calibrate.py at the real deploy cadence/noise (dt=0.1, meas=0.10, delta=0.1), or set the deployed MoverTracker and cert delta to the calibrated values (dt=0.3, meas=0.07, delta=0.3). Load the resulting q_conformal/v_eff from calib.json instead of hardcoding.
- **验证**: CONFIRMED by reading the code. Calibration: conformal_calibrate.py L35-38 sets DT_CTRL=0.30, MEAS=0.07, DELTA=DT_CTRL(=0.30); L34 comment literally states these "must match scenario.py + kf_tracker defaults so calibration == deployment"; L63 runs MoverTracker(dt=DT_CTRL, meas_noise=MEAS). out/conformal/calib.json records dt_ctrl=0.3, meas_noise=0.07, delta=0.3, and its pedestrian eps=0.05 entry yields q_conformal=0.1251, v_eff=0.6124. Deployment: render_3d_video.py L87 REPLAN_DT=LOOP["replan_dt"]=0.1 (metaurban_sando.yaml L70 replan_dt:0.1); L831 KF_MEAS_NOISE default 0.10; L655 MoverTracker(d

## [HIGH] Pedestrian-only conformal tube applied to ALL mover classes; vehicles/animals are under-covered
- **维度**: CERTIFICATE SOUNDNESS AS USED
- **位置**: `metaurban/render_3d_video.py:796-797, :937-940 (cert_clear); out/conformal/calib.json (per-class groups)`
- **问题**: MAN_QCONF=0.125 and MAN_VEFF=0.61 are the pedestrian eps=0.05 calibration values, but cert_clear applies them to every mover regardless of class (the movers tuple carries a per-class d_safe but the cert does not look up a per-class tube). calib.json shows the vehicle class needs v_eff=1.5869 (2.6x larger) at eps=0.05 because faster movers have larger CV-prediction residuals. conformal_calibrate.py's docstring explicitly states 'Deployment uses the per-class (q_conformal, v_eff) for each mover' — but deployment does not.
- **影响**: For vehicles and animals the keep-out tube is ~1 m too small at eps=0.05, so the certificate certifies clearances that the calibration does not back — collisions with non-pedestrian movers can occur at far higher than eps probability. Contradicts the stated per-class design.
- **修法**: Index q_conformal/v_eff by mover class (carry them in the kf_movers tuple from calib.json's per-class groups), exactly as EGO_PERCLASS_DSAFE is already per-class.
- **验证**: CONFIRMED by direct read. render_3d_video.py:796-797 hard-code MAN_QCONF=0.125 and MAN_VEFF=0.61, both commented "(pedestrian, CV, eps=0.05)". The active maneuver certificate render_3d_video.py:934-943 cert_clear() (called at L982/L986 inside ego_maneuver_replan, which runs at L1336) loops `for (_oid, c3, vel, r_obs, d_safe) in movers` and calls certify_horizontal(..., v_eff=MAN_VEFF, ...) and certify_above(z_clear=...+MAN_QCONF...) UNCONDITIONALLY — no per-class lookup; the tuple has no class field to switch on, and even d_safe is overridden by the global MAN_DSAFE. out/conformal/calib.json: 

## [HIGH] Wall-clock timeout (astar spam) recorded as a SAFE non-reach; 'drone in obstacle' outcome counted as non-colliding
- **维度**: Benchmark harness + metric honesty (render_screen.py parse/tally, render_3d_video.py clearance + crash=stop)
- **位置**: `metaurban/render_screen.py:41-51, 58 (timeout->pass, missing lap-done -> g() returns 'NA'); 93-101 (tally treats NA as False)`
- **问题**: On a 340s subprocess timeout the run prints no 'lap done' line, so `line=''` and every field parses to 'NA'. In the tally `tf('NA')==False`, so reached=NA counts as not-reached AND collided=NA counts as not-collided and all hit_* are False. An outcome whose own EGO log says the drone is inside an obstacle is therefore scored as a safe (non-colliding) run, and the collision count is silently not incremented.
- **影响**: For a SAFETY-layer claim, scoring an unknown/pathological run as non-colliding is favorable to ours and overstates safety. It also breaks reconciliation: ours 87 reach + 11 table-hits + 1 NA = 99 != 100, and the printed n=100 (NA still counted in denominator as an ours failure-to-reach), giving the '99 vs 100' ambiguity. The handling is asymmetric in effect: a timeout penalizes reach but never penalizes safety.
- **修法**: Treat a missing lap-done as an explicit outcome (e.g. reached=False, collided=UNKNOWN) and EXCLUDE such seeds from BOTH the reach denominator and the safety denominator, reporting the count of dropped/timed-out seeds separately. Better: detect the EGO 'drone is in obstacle' / astar-spam signature and flag it as a planner-failure category rather than folding it into 'safe'. Increase/justify the 340s cap or cap astar retries so the lap actually terminates.
- **验证**: Verified against code and data. render_screen.py:42-43 `except subprocess.TimeoutExpired: pass` swallows a 340s timeout; :45-49 leave `line=""` when no "lap done" substring exists, so `g(...)` returns "NA" for every field; :56-57 build `pc={}` for empty line, making `hit(c)` False for all classes. In the tally :93 `tf("NA")==False`, :94 `n` counts any seed having both modes (so seed90 is in the denominator), and :98-99 use `tf` so a NA row is not-reached AND all `hit_*` False = non-colliding. render_3d_video.py:1608 prints "lap done" only on normal lap completion (no finally), so a stuck/timed

## [HIGH] Unified-honesty claim '#2 mover collisions dropped from 1 to 0' is false; the fair CSV shows 3
- **维度**: CLAIMS vs CODE/DATA HONESTY AUDIT (docs/algorithm-comparisons-2026-06.md)
- **位置**: `docs/algorithm-comparisons-2026-06.md:298 ; out/conformal/render_screen.csv`
- **问题**: Line 298 states the HCT-D tracking tube landed and drove #2's mover collisions 'from 1 to 0' under the fair forward-cone protocol. The fair-protocol CSV (render_screen.csv, the one #2 is built on) shows ours mover collisions = 3, not 0. A '0' mover count only exists in the OLD, explicitly-unfair 360-static run (render_screen_360static.csv).
- **影响**: The single most load-bearing soundness claim of the HCT-D tube (that it eliminated mover collisions on the real flown path) is contradicted by the project's own fair-protocol data. A reviewer comparing the table (mover 3, doc:156) to the summary (mover 0, doc:298) will read it as either a fabricated result or borrowing the unfair-run number to credit HCT-D.
- **修法**: Rewrite line 298 to: HCT-D δ_track is wired into cert_R but #2's fair-protocol mover collisions remain 3/100 (2 animals + 1 vehicle), all high-speed/late-detection cases; do not claim '降到 0'. The '0' belongs only to the omniscient 360-static run which is no longer the headline.
- **验证**: CONFIRMED by reading the actual doc and recomputing the CSVs. (1) docs/algorithm-comparisons-2026-06.md:298 literally states "#2 的移动障碍碰撞从 1 降到 0" and "残留缺口转为静态擦碰(坠机 6...)". (2) The fair-protocol CSV that #2 is built on, out/conformal/render_screen.csv mode=ours, recomputes to hit_ped=0 hit_veh=1 hit_animal=2 -> mover_total=3 (seeds 19 animal -0.227, 25 animal -0.427, 63 veh -0.378) and hit_static=8 — exactly the #2 table row at doc:156 (ours ... | 1 | 2 | 8 | **3**). The newest fair CSV render_screen_fair_v8.csv is identical (ours mover=3). (3) The only run where ours mover=0 is render_screen_

## [HIGH] #2 honesty point '撞动物 0 / 唯一 mover 碰撞是 seed 56' contradicts its own table and the CSV
- **维度**: CLAIMS vs CODE/DATA HONESTY AUDIT (docs/algorithm-comparisons-2026-06.md)
- **位置**: `docs/algorithm-comparisons-2026-06.md:167 (vs table 156 and 162) ; out/conformal/render_screen.csv`
- **问题**: Line 167 claims '撞人 0、撞动物 0 ... 唯一 mover 碰撞是 seed 56:一辆 8.3 m/s 大货车...爬越车顶差 0.12m'. The CSV shows 撞动物 = 2 (NOT 0) and the actual mover collisions are seeds 19/25 (animals) and 63 (vehicle) — seed 56 is not a collision at all. This also self-contradicts line 162 ('ours 残留 3 个 mover 碰撞... seed 63... seed 25') and table line 156 (hit_animal=2).
- **影响**: Directly false safety claim ('clean protection of comparable-speed movers, animals 0') against the data — animals were hit twice. The seed-56 COLLDBG numbers (车顶2.8m/机体底2.68m/-0.118) are from a stale run and don't correspond to any collision in the cited CSV. Undermines the credibility of every honesty bullet.
- **修法**: Replace with the actual CSV facts: ours mover collisions = 3 (seed 19 animal -0.23, seed 25 animal -0.43, seed 63 vehicle -0.38); 撞动物 = 2, not 0; drop the seed-56 anecdote or regenerate COLLDBG against render_screen.csv.
- **验证**: Verified by reading docs/algorithm-comparisons-2026-06.md and out/conformal/render_screen.csv directly. doc:167 states '**撞人 0、撞动物 0** —— ...唯一 mover 碰撞是 **seed 56:一辆 8.3 m/s 大货车...爬越车顶差 0.12m**(...车顶 2.8m,机体底 2.68m,−0.118)'. The CSV refutes every part: (1) ours hit_animal=True for seed 19 (animal_clr −0.230) and seed 25 (animal_clr −0.430) -> 撞动物=2, not 0 (aggregate: ped=0 veh=1 animal=2 static=8). (2) ours mover collisions are seeds 19 & 25 (animals) and 63 (hit_veh, veh_clr −0.380) = 3, exactly matching table doc:156 (撞动物 2, mover 合计 3) and prose doc:162 ('ours 残留 3 个 mover 碰撞... seed 63...

## [MEDIUM] A_ENV accel-envelope deploy precondition is never implemented in the planner
- **维度**: HCT-D TRACKING TUBE + CONFORMAL CORRECTNESS
- **位置**: `metaurban/track_conformal.py:53-58,95-97; metaurban/render_3d_video.py (no A_ENV check)`
- **问题**: The calibration story depends on a deploy-time precondition that REFUSES to certify any candidate whose committed B-spline exceeds A_ENV (and leaves the >delta_track tail 'to the commit-hysteresis / accel-envelope precondition'). No such envelope check exists anywhere in render_3d_video.py / ego_bridge.py / safety_layer.py. 19.2% of harvested sub-steps are out-of-envelope (||a||>12) and tracking error blows up exactly there, but nothing prevents flying those candidates.
- **影响**: The envelope-conditional validity the calibration assumes is void at deploy. The tail the authors claim is excluded by the precondition is in fact flown, which is part of why 30% of flights breach.
- **修法**: Implement the A_ENV gate in the maneuver tournament (reject/HOLD any candidate whose committed spline peak ||a|| > A_ENV), or remove the envelope language and calibrate delta_track to cover the full unconditioned distribution.
- **验证**: VERIFIED that no A_ENV/accel-envelope gate exists at deploy. The maneuver tournament (render_3d_video.py:976-993) gates each candidate only via cert_clear() (934-943) and static_clear() (964-971); cert_clear uses R = r_obs + MAN_DSAFE + MAN_QCONF + MAN_TRACK with no acceleration check. ego_bridge.py certify_horizontal/certify_above (84-100) and safety_layer.py (129 lines) contain no envelope check. grep for A_ENV/a_env/envelope across render_3d_video.py/ego_bridge.py/safety_layer.py returns ONLY descriptive comments (render:802-804) and the calibration file. clamp_rate=0.192 confirmed in track

## [MEDIUM] Drone body radius omitted from the mover horizontal cert R (present in every other cert path), eroding the conformal budget
- **维度**: CERTIFICATE SOUNDNESS AS USED
- **位置**: `metaurban/render_3d_video.py:936 (cert_clear) vs :684/:760/:786 (other cert paths); metaurban/ego_bridge.py:72-78 docstring`
- **问题**: cert_clear builds R = r_obs + MAN_DSAFE + MAN_QCONF + MAN_TRACK with NO drone body-radius term. Every other certificate call adds the body: ego_certify_commit uses `base = r_obs + body + d_safe` (body=par.drone_radius=0.25), and slip uses `R = r_obs + body + EGO_SLIP_DSAFE`. ego_bridge.certify docstring also states R should be r_obs + r_body + d_safe + q_conformal. Because the cert is on the drone CENTER path, omitting body=0.25 means 0.25 m of the conformal+tracking margin (QCONF 0.125 + TRACK 0.29 = 0.415) is silently consumed paying for the drone's own extent, leaving only ~0.165 m of genuine prediction+tracking coverage — defeating the calibration that assumed those margins sit ON TOP of geometry.
- **影响**: The cert reports a d_safe/keep-out (MAN_DSAFE=0.45) standoff that is 0.25 m smaller at the drone surface than at its center, and the calibrated 95%/99% conformal+tracking coverage is no longer achieved at the surface. Inconsistent with the other certified modes; weakens the no-collision guarantee margin.
- **修法**: Add the body term to the maneuver cert: R = r_obs + body + MAN_DSAFE + MAN_QCONF + MAN_TRACK (and to the certify_above z_clear), matching ego_certify_commit/slip, or document that MAN_DSAFE already subsumes body and reconcile the calibration accordingly.
- **验证**: VERIFIED by reading the code. render_3d_video.py:936 builds the maneuver mover cert as `R = r_obs + MAN_DSAFE + MAN_QCONF + MAN_TRACK` with NO body term, while every other mover cert path includes body=par.drone_radius: ego_certify_commit at :684 `base = r_obs + body + d_safe`, and slip paths at :716/:760/:786 `R = r_obs + body + EGO_SLIP_DSAFE` (body defined at :679/:711/:745/:779; drone_radius=0.25 in metaurban_sando.yaml:33). The certificate proves center-to-center separation ||p(t)-c(t)|| >= rho(t)=R+v_eff*(t+delta) where p(t) is the drone-CENTER B-spline (bernstein_cert.hpp:311-323; ego_b

## [MEDIUM] EGO_VEFF env var aliases two distinct calibrated knobs (maneuver tube 0.61 vs slip tube 0.25)
- **维度**: CERTIFICATE SOUNDNESS AS USED
- **位置**: `metaurban/render_3d_video.py:423 and :797`
- **问题**: EGO_VEFF_SLIP and MAN_VEFF both read os.environ.get("EGO_VEFF", ...) but with different defaults (0.25 vs 0.61). Setting EGO_VEFF to tune one silently overrides the other, including overriding the conformal-calibrated 0.61 maneuver tube with an uncalibrated value (or vice versa).
- **影响**: An operator/experiment setting EGO_VEFF for slip silently shrinks the maneuver keep-out tube (or enlarges it), invalidating the conformal coverage of whichever mode they were not thinking about. Reproducibility/soundness hazard for the reported results.
- **修法**: Give the two knobs distinct env names (e.g. EGO_VEFF_MAN and EGO_VEFF_SLIP).
- **验证**: Confirmed verbatim. render_3d_video.py:423 `EGO_VEFF_SLIP = float(os.environ.get("EGO_VEFF", 0.25))` and :797 `MAN_VEFF = float(os.environ.get("EGO_VEFF", 0.61))` both read the same env var with different defaults. They are genuinely distinct certificate knobs: EGO_VEFF_SLIP feeds the SLIP cert at :765/:788 (`v_eff=EGO_VEFF_SLIP / s`) and MAN_VEFF feeds the maneuver cert at :937/:938 (`v_eff=MAN_VEFF`). v_eff directly grows the certified keep-out tube (ego_bridge.py:75-76,86: `rho(t)=R+v_eff*(t+delta)`), and MAN_VEFF is documented as "CONFORMAL-CALIBRATED tube growth (pedestrian, CV, eps=0.05)

## [MEDIUM] 'P(collision) <= eps' is marginal per-instance coverage, not the joint over-the-window coverage the continuous-time cert needs
- **维度**: CERTIFICATE SOUNDNESS AS USED
- **位置**: `metaurban/conformal_calibrate.py:8-21, :109-131 (per (track,t_k,Delta) instances, marginal coverage); render_3d_video.py:937-940 (cert claims all-t)`
- **问题**: The Bernstein cert proves ||p(t)-c_pred(t)|| >= rho(t) for ALL t in [0,t_hi] jointly. To turn this into P(collision)<=eps you need the prediction residual to stay inside the tube jointly over the whole window for an encounter. But the conformal calibration scores residuals as independent (track, detection-time, horizon) instances and validates MARGINAL coverage (test_marginal_coverage ~0.95). Marginal per-instance coverage does not imply per-encounter joint coverage over continuous t; the true collision probability over a window is higher than the per-instance eps.
- **影响**: Overclaim of the probabilistic guarantee for the mover keep-out: the headline P(collision)<=eps is a per-instant marginal statement, weaker than the per-encounter guarantee implied by a continuous-time certificate.
- **修法**: Calibrate the mover tube with a per-track (per-window) SUP score s = max_t e(t)/(q+v_eff*t) like track_conformal.py already does, so the conformal quantile backs the all-t cert; or restate the claim as per-instant marginal coverage.
- **验证**: Confirmed by reading the cited code. (1) The certificate is a JOINT all-t guarantee over the window: ego_bridge.py:73-78 (certify) "for ALL continuous t up to t_hi", tube rho(t)=R+v_eff*(t+delta); :85-87 certify_horizontal "for ALL ... t_hi"; :94-95 certify_above "for ALL t up to t_hi". Wired at render_3d_video.py:936-940 with t_hi=EGO_TAU_TRUST, v_eff=MAN_VEFF, delta=REPLAN_DT. (2) The MOVER tube is calibrated as MARGINAL per-instance coverage: conformal_calibrate.py:9-11 instances=(track, detection-time t_k, horizon Delta); :73-78,162-167 one residual e(Delta)=||true_future-pred|| per instan

## [MEDIUM] Per-class collision breakdown undercounts collisions by 1 per mode (2-decimal rounding vs full-precision collided)
- **维度**: Benchmark harness + metric honesty (render_screen.py parse/tally, render_3d_video.py clearance + crash=stop)
- **位置**: `metaurban/render_screen.py:55-57 (pc parse + hit lambda); render_3d_video.py:1605-1606 (lap-done print)`
- **问题**: `collided` is derived from full-precision min_clr (`collided={mclr < 0}`), but the per-class hit_* flags are parsed from the per_all values that are printed with `.2f`. A grazing collision whose clearance is in (-0.005, 0) rounds to the string '-0.00', parses to float -0.0, and `hit = pc[c] < -1e-6` is False. So a row can be collided=True with NO per-class flag set, and the aggregate 撞人/撞车/撞动物/坠机 table sums to fewer than the true collision count.
- **影响**: The headline per-class safety table (the 撞人/撞车/撞动物/坠机 counts the summary prints) systematically undercounts real collisions. A reader summing the table gets 11 ours / 29 ego collisions, hiding 1 collision in each mode. Reconciliation fails (reach+table_hits != n). The exact '12 collided vs 11 flagged' discrepancy the review asked about.
- **修法**: Do not re-parse rounded display strings for the safety metric. Either (a) print per_all with enough precision (e.g. `:.4f`) and lower/remove the -1e-6 threshold, or (b) emit a machine-readable lap-done summary (the per-class clearances as raw floats, e.g. JSON) that render_screen parses, and define hit_* and collided from the SAME precision/threshold. Also assert per-frame that `collided == any(hit_*)` so the inconsistency is caught.
- **验证**: CONFIRMED by reading code + data. render_3d_video.py:1608-1609 prints collided={mclr < 0} (full-precision float) but per-class clearances via "  ".join(f"{k}:{v:.2f}" ...) at .2f precision. clearance() (1081-1097) returns gmin=min(per.values()); 1537-1538 set mclr=min over t of gmin and per_all[k]=min over t of per[k], so mclr==min(per_all.values()) — the class hitting the global min carries that value into per_all, and if mclr in (-0.005,0) its .2f print is "-0.00". render_screen.py:55-57 parses pc from that .2f string and hit=lambda c: bool(c in pc and pc[c] < -1e-6); float("-0.00")=-0.0 whi

## [MEDIUM] #2 conformal status overclaimed as calibrated; code uses uniform pedestrian scalars, no P(coll)<=eps
- **维度**: CLAIMS vs CODE/DATA HONESTY AUDIT (docs/algorithm-comparisons-2026-06.md)
- **位置**: `docs/algorithm-comparisons-2026-06.md:56 (vs 169/296) ; metaurban/render_3d_video.py:796-797,937-939`
- **问题**: The summary table (line 56) lists #2's conformal as 'mover keep-out: conformal q + HCT-D δ_track（标定）', implying calibrated probabilistic keep-out. The code applies fixed pedestrian-eps0.05 scalars (MAN_QCONF=0.125, MAN_VEFF=0.61) and MAN_TRACK=0.29 UNIFORMLY to all mover classes (ped/vehicle/animal). It is not per-class calibrated and yields no P(coll)<=eps. This contradicts the doc's own honesty lines 169 ('MAN_QCONF 是 placeholder...非 P(碰)≤ε') and 296.
- **影响**: A skeptical reviewer attacking 'P(collision)<=eps' will correctly note #2 carries the eps-label in the table but is a deterministic geometric margin borrowed from one class. Only #1 (calib.json, held-out coverage matched) is a real conformal guarantee. The table conflates the two tiers.
- **修法**: In the line-56 table, mark #2 conformal as 'geometric (pedestrian q/v_eff borrowed, not per-class) + δ_track' or 'placeholder', consistent with lines 169/296. Reserve '标定 / P(碰)≤ε' for #1 only.
- **验证**: All load-bearing facts verified by reading the code/doc/data directly. docs/algorithm-comparisons-2026-06.md:56 verbatim lists #2's conformal as "mover keep-out:conformal q + **HCT-D δ_track**(标定)" — and unlike rows #4/#5 (lines 58-59 "0.0 placeholder(几何)"), it carries no placeholder/geometric caveat, using the "conformal q" name + "(标定)/calibrated" label. This contradicts the doc's own honesty: line 169 "MAN_QCONF 是 placeholder(几何 margin,非 P(碰)≤ε)" and line 296 "#2/#4/#5 的 keep-out 多是确定性几何 margin(q_conformal/MAN_QCONF 为 placeholder)". Code: render_3d_video.py:796-798 define MAN_QCONF=0.125, M

## [LOW] Mover perception is NOT literally identical: native is fed clean GT boxes, ours is fed noisy-detection + KF estimates
- **维度**: FAIR-COMPARISON PERCEPTION SYMMETRY
- **位置**: `metaurban/render_3d_video.py:1023-1028 (native fov_cloud) vs 652-664 (ours kf_movers)`
- **问题**: The claim 'ours and native perceive through the IDENTICAL forward cone' is literally false for movers. Native EGO is fed each mover's EXACT ground-truth position and box; ours is fed a noisy detection passed through a Kalman filter (smoothed centre + KF lag). So the two planners do not perceive the same mover state — only the same cone and the same detection set.
- **影响**: Breaks the strict 'identical perception' wording. Direction is benign — native sees CLEANER mover data, so this disfavors ours (ours must overcome detection noise + KF lag). It therefore does not inflate ours' result; if anything it makes ours' safety win more impressive. But the slogan as written is an overclaim.
- **修法**: Either feed native the same noisy detection stream (true symmetry), or rescope the public claim to 'identical forward-cone geometry, static occupancy, and mover-detection set; mover state estimation (KF) is part of the safety layer.'
- **验证**: Factual basis CONFIRMED by reading the code. Native (render_3d_video.py:1023-1025 fov_cloud): `for oid, cls, pos, vel, size in native_objects(): if cls=="static": continue; if _seen(p3(pos, size[2]*0.5)): chunks.append(np.asarray(_voxel_box(pos, size), float))` — GT `pos` fed directly, no noise/filter. Ours (652-663 kf_movers): `det = np.asarray(c3, float) + _KF_RNG.normal(0, KF_MEAS_NOISE, 3)`, `trk.update(det)`, `kc0, kv, _ka = trk.state()`, then `out.append((oid, kc0, ...))` feeds the KF-smoothed `kc0`, not GT `c3`. Both originate from the same `native_objects()` GT (636-639) and the same c

## [LOW] kf_movers retains a live 360-degree (SENSE_R) fallback and is re-invoked off the cone on diagnostic/slip paths
- **维度**: FAIR-COMPARISON PERCEPTION SYMMETRY
- **位置**: `metaurban/render_3d_video.py:634-635, 712, 748, 780, 1542; ego_safety_obstacles 556-571`
- **问题**: kf_movers defaults to omniscient 360-degree detection when cam_heading is None (`np.linalg.norm(c3[:2]-p_d[:2])<=SENSE_R`). The maneuver A/B always passes cam_heading (905) so the cone is honored, but several call sites still invoke it without a heading: the --slip occupancy/cert (712/748/780), the crash-diagnostic COLLDBG (1542), and the older --ego_safe gate `ego_safety_obstacles` is SENSE_R-based on GT (556-571). Any of these reintroduces omniscient mover detection if used in an A/B.
- **影响**: Low for the current maneuver-vs-ego headline (those paths are not on the per-tick maneuver decision). But it is a latent footgun: a future reuse of kf_movers() without cam_heading, or running --slip / --ego_safe as the A/B, silently gives ours 360-degree omniscient mover perception that native lacks — which WOULD rig the comparison. Also, with MAN_COLLDBG=1 the crash-frame re-call (1542) re-runs the noisy KF update on a 360 set, double-updating the persistent filters at the crash instant (diagnostic-only, results already broken by then).
- **修法**: Make cam_heading a required argument (or assert it is not None) so omniscient detection cannot be requested by accident; route the slip/ego_safe gates through the same cone; have COLLDBG read the already-computed `movers` list instead of re-calling kf_movers.
- **验证**: Every cited line verified by personal read. render_3d_video.py:634-635: kf_movers' detector is `seen = (lambda c3: _in_cone(c3,p_d,cam_heading)) if cam_heading is not None else (lambda c3: np.linalg.norm(c3[:2]-p_d[:2]) <= SENSE_R)` — a 360-degree SENSE_R disk when cam_heading is None. The maneuver headline honors the cone: line 905 `movers = kf_movers(p_d, t_sim, cam_heading)` with cam_heading=quad.yaw (904). The 360 fallback is exercised by: _slip_mover_cloud (712), ego_speed_search (748), ego_slip_feasible (780) — all call kf_movers(p_d, t_sim) with NO heading; gated by args.slip (1331, 137

## [LOW] MAN_DEADBAND / _MAN_STATE hysteresis is dead code — never read; root cause of seed-25 maneuver thrashing
- **维度**: SPEED-FOV COUPLING SOUNDNESS
- **位置**: `metaurban/render_3d_video.py:834-837, 976-993, 1320, 1512-1516; metaurban/safety_layer.py:101-113`
- **问题**: The constant MAN_DEADBAND and the _MAN_STATE dict are documented as anti-chatter hysteresis ('keep the CURRENT maneuver unless another certified one beats its goal-ward speed by >this ... Stops the around-L/R/over flicker'), but MAN_DEADBAND is never referenced after its definition and _MAN_STATE['kind'] is only ever WRITTEN (lines 990, 1320), never read. The tournament (lines 977-989, and the canonical copy safety_layer.maneuver_decide:101-113) is fully stateless: it re-runs every tick in fixed order straight->around_l(+25)->around_r(-25)->around_l(+50)->around_r(-50) and breaks at the first candidate that certifies. With per-mover KF detection noise (KF_MEAS_NOISE=0.10) the first cert-clearing candidate flips between around_l and around_r tick-to-tick, and each flip is counted as a switch (lines 1514-1515). That is exactly the switches=101 / no-reach 'thrashing'.
- **影响**: The advertised anti-flicker mechanism does not exist. Under any perception noise the L/R branch flip-flops every replan tick, producing brake-then-reaccelerate hesitation, no net progress to goal, and the seed-25 failure (switches=101, no reach). It is simultaneously a correctness bug (no hysteresis) and an overclaim (comments assert hysteresis is implemented).
- **修法**: Actually wire the hysteresis: before adopting a new 'chosen', compare its goal-ward speed (or arc-length progress) against the previously chosen kind stored in _MAN_STATE['kind'], and keep the previous kind unless the new one beats it by > MAN_DEADBAND. E.g. evaluate all candidates, score by goal-ward speed, and only switch away from _MAN_STATE['kind'] when best_other - score(prev) > MAN_DEADBAND. Apply identically in safety_layer.maneuver_decide so the library and the renderer agree.
- **验证**: CODE CLAIM CONFIRMED. metaurban/render_3d_video.py:834-836 defines MAN_DEADBAND=0.5 with a comment promising hysteresis ("keep the CURRENT maneuver unless another certified one beats its goal-ward speed by >this... Stops the around-L/R/over flicker"). Repo-wide grep: MAN_DEADBAND appears ONLY at line 834 (definition) — never read anywhere. _MAN_STATE appears only at 837 (def), 990 (write `_MAN_STATE["kind"] = chosen`), 1320 (reset `_MAN_STATE["kind"] = None`) — write-only, never read. The tournament at 977-983 is stateless: fixed order straight→around_l(+PHI)→around_r(-PHI)→around_l(+2PHI)→aro

## [LOW] Stopping-distance cap does not guarantee avoidance: warp scales speed not braking force, and FOV_GMIN floor means it never stops
- **维度**: SPEED-FOV COUPLING SOUNDNESS
- **位置**: `metaurban/render_3d_video.py:599-601, 1389-1403, 1435-1441, 604-608`
- **问题**: The docstring claims the cap ensures 'the braking distance fits inside [the perceived free distance]'. v_allow=sqrt(2*FOV_ABRAKE*(d_free-FOV_DMARGIN)) with FOV_ABRAKE=4.0 assumes a 4 m/s^2 brake. But the executor never commands that brake — it time-warps the SAME B-spline (ego.eval(t_ego+g*DT), sp_vel*g, sp_acc*g^2). The achieved deceleration is whatever the warp+quad dynamics produce, not the assumed 4.0. Worse, g_raw is clipped to FOV_GMIN=0.15 (line 1397, np.clip(..., FOV_GMIN, 1.0)), so when d_free<=FOV_DMARGIN=1.2 m -> v_allow=0 -> g is still floored at 0.15. The drone keeps crawling at 0.15*vpk (up to ~0.9 m/s at vpk~6) straight toward the obstacle; the cap can never bring it to a stop. So braking distance is NOT guaranteed to fit inside d_free.
- **影响**: The 'don't outrun the sensor' property is asserted but not delivered. For a late-detected static obstacle directly on the committed path, the cap slows the drone to a non-zero crawl but does not stop it; avoidance relies entirely on static_clear()+replan firing in time. The formula is a guarantee on paper, a soft heuristic in code — an overclaim that should not be cited as a safety property.
- **修法**: Either (a) actually enforce the brake: when v_allow< current flown speed, command a deceleration set-point (modify sp_acc toward -FOV_ABRAKE along the path) rather than only scaling the spline; and/or (b) allow g to reach 0 (set FOV_GMIN=0 for the static-stop case, or hold/hover when d_free-FOV_DMARGIN<=0) so the drone can actually stop; and (c) re-word the docstring to 'reduces speed proportional to perceived free distance (heuristic), not a certified stopping guarantee.'
- **验证**: The mechanical claims are all verifiable-true. render_3d_video.py:599 sets FOV_ABRAKE=4.0; :1397 computes v_allow = (2.0*FOV_ABRAKE*max(d_free-FOV_DMARGIN,0))**0.5 (the classic stopping-distance formula assuming a 4 m/s^2 brake). The executor (:1435-1444) is a pure time-warp: s = ego.eval(min(t_ego + ego_speed_g*DT, ...)); sp_vel *= ego_speed_g; sp_acc *= ego_speed_g**2 — it never commands a deceleration set-point, so the assumed 4 m/s^2 brake is not enforced. :1400 clips g to FOV_GMIN=0.15 (:601, comment "keep crawling, never dead-stop here"), so when d_free<=FOV_DMARGIN=1.2 -> v_allow=0 -> g

## [LOW] Hodograph 'shape' covariate model is degenerate (c1≈c2≈0); the g(feat) machinery is inert
- **维度**: HCT-D TRACKING TUBE + CONFORMAL CORRECTNESS
- **位置**: `metaurban/track_conformal.py:66-73,106; docstring lines 8-10`
- **问题**: The narrative is that tracking error is driven by ||a|| and lateral/curvature ('collisions live in high-accel/high-curvature evasive turns, NOT peak speed'). The NNLS fit drives both covariate weights to ~0, so g is effectively the constant c0=0.02 (floored), and kappa just rescales a constant. The whole sup-normalized shape model contributes nothing.
- **影响**: The 'model-free hodograph tube' framing oversells a model that is essentially a constant. It does not invalidate delta_track (which is computed in raw metres, independent of g), but the kappa/tube_med diagnostics and the 'shape' story are not supported by the data.
- **修法**: Either drop the covariate-shape narrative and report a plain constant-margin conformal bound, or refit with covariates that actually correlate (and demonstrate variance reduction) before claiming an accel/curvature-aware tube.
- **验证**: I read track_conformal.py and the actual calib + harvest data, and recomputed the statistics from the 99 harvest files (47,715 sub-steps).

CONFIRMED facts:
- track_calib.json:2-6 shows exactly c0=0.02, c1=4.588e-4, c2=7.868e-4 as the finding states.
- c0=0.02 is the FLOOR injected at track_conformal.py:69 (`c[0] = max(c[0], 0.02)`). I reran the raw NNLS on the in-envelope data: it returns [c0=0.00088, c1=4.589e-4, c2=7.869e-4]. So the fit's own constant was ~0 and got floored up to 0.02.
- Empirical correlations match exactly: corr(delta,||a||)=+0.24, corr(delta,||v||)=-0.053. Critically, WIT

## [LOW] delta_track harvested under time-warped (slowed) set-points and added to both H and V keep-outs
- **维度**: HCT-D TRACKING TUBE + CONFORMAL CORRECTNESS
- **位置**: `metaurban/render_3d_video.py:1440-1441,1456-1459; 924,936,939`
- **问题**: (a) The harvested set-point velocity/accel are scaled by ego_speed_g (sp_vel*=g, sp_acc*=g^2) before quad.step, so delta=||quad.p - sp_pos|| is the tracking error to a SLOWED set-point; a full-speed certified candidate would track worse, so the calibration can under-estimate tracking error for fast candidates. (b) The 3D-norm delta_track is added in full to BOTH the horizontal radius cert_R and the vertical z_clear, assuming the error can be simultaneously fully horizontal and fully vertical.
- **影响**: (b) is conservative (not a soundness bug) but inflates z-clearance unnecessarily. (a) is a real but minor sampling bias that compounds the under-sizing already identified above. Note MAN_QCONF (prediction error) + MAN_TRACK (tracking error) are distinct error sources, so adding both is NOT double-counting.
- **修法**: Harvest tracking error against the untimed (g=1) committed spline, or decompose delta_track into horizontal and vertical components and add each to its own keep-out direction rather than the full 3D norm to both.
- **验证**: Both sub-claims verified against code I read. (a) render_3d_video.py:1443 `sp_vel = np.asarray(sp_vel,float)*ego_speed_g` and :1444 `sp_acc = ...*(ego_speed_g**2)`; position via time-warped param at :1438 (`min(t_ego+ego_speed_g*DT,...)`); quad tracks these at :1458 `quad.step(sp_pos,sp_vel,sp_acc,DT,...)`; harvest at :1462 `float(np.linalg.norm(quad.p - sp_pos))` with covariates nv/na/lat computed from the *scaled* sp_vel/sp_acc (:1460-1461). So the tracking error and its covariates are genuinely harvested under slowed set-points — exactly as described (cited 1440/1441/1459 are line-drifted; 

## [LOW] Maneuver may fly a STALE trajectory unre-certified when all replans fail
- **维度**: CERTIFICATE SOUNDNESS AS USED
- **位置**: `metaurban/render_3d_video.py:984-992, :1338, :1345`
- **问题**: If all tournament replans fail (including the climb replan at L989), EGO retains its last good trajectory (ego_traj duration > 0) but cert_clear was only ever run on freshly-replanned candidates, never on the retained old trajectory against the CURRENT (moved) movers. The main loop then flies that stale plan (ego_dur>1e-3) with no re-cert.
- **影响**: Edge case where a previously-certified path is flown after the world has moved, without re-certification — a smaller version of the uncertified-flight problem in finding #1.
- **修法**: On total replan failure, force HOLD/hover rather than flying the retained trajectory, or re-run cert_clear() on the retained trajectory against current movers before flying it.
- **验证**: Verified by tracing the code. render_3d_video.py:989 `ego.replan(p_d, v_d, a_d, np.array([p_d[0], p_d[1], z_top])); chosen = "climb"` discards the replan return value and runs NO cert_clear()/static_clear(), unlike the tournament (L982) and "over" (L985-986) branches which gate on `cert_clear() and static_clear()`. cert_clear() (L934-943) certifies the CURRENTLY-held B-spline but is only invoked inside those tournament/over conditionals, never on the climb/stale path. On replan failure, the prior trajectory is retained: ego/src/planner_manager.cpp reboundReplan calls updateTrajInfo() only on t

## [LOW] Summary tallies over all seeds though docstring claims a 'navigable set where OURS succeeds'
- **维度**: Benchmark harness + metric honesty (render_screen.py parse/tally, render_3d_video.py clearance + crash=stop)
- **位置**: `metaurban/render_screen.py:3-6 (docstring) vs 90-101 (main tally)`
- **问题**: The module docstring states the A/B is summarised on the subset of seeds where ours reaches with 0 collisions ('navigable, not static-blocked'). main() does no such filtering: it counts n = seeds with both modes and sums reach/hits over ALL of them. The stated experimental protocol is not what the code computes.
- **影响**: Mismatch between described methodology and implementation. Here the code is actually MORE honest (full-set), but any paper/claim citing 'navigable-set A/B' would be unsupported by this harness, and the printed n/reach numbers can be mis-read as a controlled set when they are the raw full set including static-blocked seeds.
- **修法**: Either implement the navigable filter (and report both raw and filtered tables) or update the docstring to state the summary is the full 100-seed set. State explicitly which seeds (if any) are excluded and why.
- **验证**: Verified by reading render_screen.py. Docstring lines 4-5 state: "select the seeds where OURS succeeds (reach + 0 collision = navigable, not static-blocked) and summarise the A/B on that fair set." main() does no such filtering: line 90-92 builds `by` from all rows; line 94 `n = sum(1 for d in by.values() if "ours" in d and "ego" in d)` only requires both modes ran; lines 98-99 (`col`/`re_`) iterate `for s in by if who in by[s]` with no reach/collision predicate. grep for navigable/fair set across metaurban/ returns ONLY these docstring lines — the filter exists nowhere and no downstream consu

## [LOW] feed_native() omits static obstacles from the clearance metric (latent unfair A/B if MIT-ACL native is benchmarked)
- **维度**: Benchmark harness + metric honesty (render_screen.py parse/tally, render_3d_video.py clearance + crash=stop)
- **位置**: `metaurban/render_3d_video.py:1059-1078 (feed_native) vs 517-546 (feed adds STATIC_FED at 529)`
- **问题**: feed() (used by the EGO branch for both ours and native in render_screen) appends static objects to `fed` (line 529), so clearance() charges static collisions. feed_native() (MIT-ACL SANDO path) never adds static to `fed`. If the screen harness or any A/B ever routes native through the `native is not None` branch, native's min_clr and hit_static would ignore trees entirely (hit_static can never fire), making native look collision-free against statics it actually clips.
- **影响**: Not exercised by render_screen (run_one always passes --ego -> feed()), so current numbers are unaffected. But it is a latent metric-fairness landmine: a future ours-vs-MIT-ACL-native comparison would credit native with zero static collisions by construction.
- **修法**: Add the same `fed.extend(... STATIC_FED ...)` to feed_native() so the clearance metric is identical regardless of which planner produced the path, and add a test asserting feed()/feed_native() return the same static entries for a fixed pose.
- **验证**: Confirmed by reading the code. feed() charges GT statics in the clearance metric: render_3d_video.py:529 `fed.extend((c, c3, sz) for (c, c3, sz) in STATIC_FED if np.linalg.norm(c3[:2] - p_drone[:2]) <= SENSE_R)`, where STATIC_FED entries are `("static", p3(pos, h*0.5), size)` built from GT native_objects() at build_static_field (line 495). feed_native() (render_3d_video.py:1059-1078) starts `fed=[]` (1065), loops native_objects() skipping cls=="static" (1067) and animals (1072-1076), and never extends with STATIC_FED -> returns a fed list with zero static entries (1078). clearance() (1081-1097

## [LOW] Stale '92 vs 71' in unified honesty contradicts the #2 table's 87 vs 70
- **维度**: CLAIMS vs CODE/DATA HONESTY AUDIT (docs/algorithm-comparisons-2026-06.md)
- **位置**: `docs/algorithm-comparisons-2026-06.md:299 (vs 156) ; out/conformal/render_screen_track0.csv vs render_screen.csv`
- **问题**: Unified-honesty line 299 cites '#2 的 92 vs 71' as the safe-arrival headline, but #2's own results table (line 156) and the current render_screen.csv give 87 vs 70. The 92/71 figure matches the superseded render_screen_track0.csv.
- **影响**: Inconsistent headline arrival numbers within the same document; a reviewer cross-checking will distrust the whole #2 section. Also '撞人 0' is true but appears alongside the false '撞动物 0' implication from line 167.
- **修法**: Change line 299 to '#2 的 87 vs 70 + 撞人 0' to match the current CSV and the #2 table.
- **验证**: All four sub-claims verified by reading the actual doc and recomputing the CSVs.

(1) docs/algorithm-comparisons-2026-06.md:299 literally reads "#2 的 92 vs 71 + 撞人 0" (confirmed via Read and grep). (2) The #2 results table at line 156 reads "ours(同锥感知) | 87/100" and "native EGO(同锥感知) | 70/100"; the same 87 vs 70 headline is repeated at line 56 (summary table) and line 160 ("到达 87 vs 70"). So line 299 is internally inconsistent with three other places in the same document.

(3) Recompute of the current out/conformal/render_screen.csv (mtime Jun 26 23:53, 100 rows/mode): ours reached(True)=87, e

## [LOW] Stale CSV-shape description: '199 行 / 99 对 / seed 80 缺 ours' — actual file has 200 rows, 100 complete pairs
- **维度**: CLAIMS vs CODE/DATA HONESTY AUDIT (docs/algorithm-comparisons-2026-06.md)
- **位置**: `docs/algorithm-comparisons-2026-06.md:131,144 ; out/conformal/render_screen.csv`
- **问题**: Lines 131 and 144 describe render_screen.csv as '199 行 = 99 对 + 1 孤儿 ego (seed 80 无 ours 行)' and 'n=99 对'. The current file has 200 data rows, 100 ours + 100 ego, all 100 seeds present for both modes including seed 80. The /100 denominators in the table (87/100, 70/100) already assume 100.
- **影响**: Minor but it's a factual mismatch in the run-conditions table; signals the section was not re-synced after the final 100-seed rerun (same root cause as the 92/71 and seed-56 staleness).
- **修法**: Update to '200 行 = 100 对完整 ours/ego (seeds 0-99)'; remove the seed-80-orphan note.
- **验证**: Confirmed by reading both files directly. docs/algorithm-comparisons-2026-06.md:131 states "199 行 = 99 对完整 ours/ego + 1 个孤儿 ego(seed 80 无 ours 行)" and :144 states "seed 80 ours 无 lap-done 行 → n=99 对". The actual out/conformal/render_screen.csv has 201 lines incl header = 200 data rows. awk counts: mode=ours 100 rows / 100 unique seeds / 0 dups; mode=ego 100 rows / 100 unique seeds / 0 dups; seed range 0-99. Seed 80 has BOTH rows (no orphan): "80,ego,True,...,9.8" and "80,ours,True,...,10.7" — directly refuting the "seed 80 无 ours" claim. So the truth is 200 rows = 100 complete pairs (seeds 0-9

## [LOW] Per-class hit attribution under-counts static crashes by 1 each due to 3-decimal rounding threshold
- **维度**: CLAIMS vs CODE/DATA HONESTY AUDIT (docs/algorithm-comparisons-2026-06.md)
- **位置**: `metaurban/render_screen.py:55-57,60-63 ; out/conformal/render_screen.csv`
- **问题**: The global `collided` flag is parsed straight from render_3d_video, but per-class hit flags are derived from per-class clearances re-parsed at 3-decimal precision with hit = clr < -1e-6. Two collided episodes (ours seed 15, ego seed 39) have static_clr rounded to -0.000 so hit_static stays False, leaving the collision unattributed. Thus the table's static counts (8 ours / 19 ego) are really 9 / 20, and collided totals (12 / 30) exceed the per-class sum (11 / 29).
- **影响**: Does not affect the headline mover claim (3 vs 10 is robust — both orphans are static near-misses), but the '坠机 8 vs 19' row is a slight undercount and the class split is not collision-conservative. A careful reviewer summing the split will notice it doesn't equal the collided column.
- **修法**: Attribute by argmin clearance when collided=True but no class flag fires (assign the most-negative per-class clr, or parse the unrounded value), so per-class hits sum to the collided total; report static as 9/20.
- **验证**: CONFIRMED with code + data I read personally.

Mechanism: render_screen.py:50 parses the global collided flag straight from the render line (`coll = g(r"collided=(\w+)")`), while render_screen.py:55-57 derives per-class hits from the per-class clearance parsed from the same line: `pc = {c: float(m.group(1)) ...}; hit = lambda c: bool(c in pc and pc[c] < -1e-6)`. In render_3d_video.py:1608-1609 the line prints `collided={mclr < 0}` at full precision but each per-class clearance as `f"{k}:{v:.2f}"`, so any per-class clearance in (-0.005, 0) prints as `-0.00`, parses to -0.0, and fails `< -1e-6`.
