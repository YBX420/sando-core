# 进展整理:2026-06-24 ~ 06-25(认证安全层)

> 两天 47 个提交、8 个 memory。主线:把"认证的最快+最安全绕行(certified go-around)"从算法做到**真 MetaUrban 场景 + 真四旋翼动力学下经得起验证**,并搭好可复用的评测/标定/研究基础设施。

---

## 一句话最终结论
在 **100 个真 MetaUrban 场景 + 真四旋翼动力学**、**碰撞=坠机=不到达** 的 realistic 语义下:
- **ours(EGO + 认证安全层):安全完成 92/100、坠机 7**
- **native EGO:安全完成 71/100、坠机 29**
- 到达头对头 **ours +23**(native 坠机处 ours 安全到达)vs native +2。

native 原始"100 到达"里 29 个是撞着冲过去的;realistic 语义下 ours 反超 21 个。

---

## 2026-06-24(昨天)

### 1. Conformal 标定补齐 P(碰)≤ε(里程碑)
- 把占位的 `q_conformal=0` / 手设 `v_eff=0.2` 换成**真 MetaUrban 轨迹上 split-conformal 标定**的分布无关 keep-out,test 覆盖率达标 —— 项目首次有 P(碰)≤ε 实测依据。
- **CV 预测器砍半行人 keep-out**(0.61 vs CA 1.29)。
- 真轨迹 A/B:**ours 0/120 碰撞 vs native 19**,用时中位更快、全 ≤native+3s。
- **连续证书 0 false-safe vs 离散 2 点漏 202/367** = soundness 硬证据。
- D435i 深度相机接入(--d435i 真深度反投影喂 EGO)。
- 管线:`conformal_{harvest,calibrate}` / `replay_core` / `cert_ablation` / `predictor_compare`。

### 2. 机动模式
- **KF 预测圆柱机动层**(直走/绕/飞越/爬升锦标赛)+ 空间-时间 **SLIP**(速度-warp)。
- 渲染-忠实动力学:`--dynamics` 把 setpoint 过真四旋翼模型飞。

### 3. PX4 SITL 闭环跑通
- ours 在**真 PX4** 飞通走廊到达+0碰撞。整晚"在 PX4 上输"全是**基础设施 bug 非算法**(stale lockstep / 电池 60s 耗光 / setpoint 高度轴 bug / `px4 alive` 假阴性 / EKF 没收敛就 arm)。
- `px4_sitl.sh`(启停脚本)+ `px4_bridge`(等满 health + arm 重试)+ `px4_batch`(弹性批验证)。

### 4. SANDO baseline + 三方对比
- native MIT-ACL SANDO 头less 飞起来(周期 replan + 跟随承诺路径)。
- `compare3`:ours/EGO/SANDO 等速扫描(3-8 m/s)→ 可复用 CSV。

### 5. 延时 + 一个 10000× 提速
- 每层延时测量:KF~0.03ms、EGO replan~1ms、横向证书 0.038ms。
- **竖直证书 certify_above 76ms→7μs(~10000×)**:`g_seg_worst_deficit` 加 `lo_min>0` 早退(sound),ctest 25/25。
- 整 tick 15ms = 控制预算 5%,20× 实时余量。

### 6. Planner-无关地基
- `cert_capi`(证任意 BSeg,脱离 EGO)+ `cert_bridge` 适配器(幂基→Bernstein/B样条/Bezier)。
- 66 个可移植 planner 调研(GCOPTER/mav_traj/MPL 领先)。
- **5 个 planner 表示嫁接同一证书全 sound**。
- reliability chunked driver(绕开 EGOPlanner 每 ep 漏 8MB 内存)。

---

## 2026-06-25(今天)

### 7. D435i 规划失败诊断+修复(达 GT 水平)
- **4 个独立根因全是基础设施非算法**:① `perceive()` 渲染后恢复相机位姿→反投影用错外参→点云恒朝 +X;② 相机挂机体 yaw 而非行进 heading;③ maneuver 没裁剪到 EGO_HOR;④ 稠密深度云撑爆 A* 0.2s 预算。
- seed3 修到 reached 13.2s/0碰/clr0.53(GT 11.1s/0.42)。

### 8. Planner-无关安全层 headless 矩阵
- 4 个 planner(ego/quintic/septic/bspline)× 有无安全层。**每个 safety=on 全速度 0 碰撞**;裸飞撞 7-12%。

### 9. GCOPTER vendored
- 去-ROS 化(OMPL 引导路径换自写 A*,FIRI+MINCO 核不动)+ C-API。standalone 能绕障可认证;FIRI 在动态云上崩,不进矩阵。

### 10. render==headless 一致性根治(关键)
- 诊断"渲染好 headless 差"的真因:**不是 PX4、不是动力学**(真动力学反而让 ego 撞更少),是**渲染多了整个 3D 静态场景** + 两套代码参数漂移。
- **`render_3d_video --headless`**:同 sim/planner/安全层/真四旋翼,跳渲染 → 和渲染**逐字节一致**,~34s vs 3-4min,纯 CPU 可 10× 并行 → **render 和 headless 成一份代码 = 天然一致**。
- `safety_layer.py`:共享决策(headless 半边统一)。

### 11. 诚实 100 场景基准 + 一步步做到完胜
- `render_screen.py`(ThreadPool 并发):100 场景 ours vs native。
- **静态修复**(360°全向静态 + around± 绕行 + 静态净空门):碰撞 **26→10**。根因:证书只护 mover,fov_cloud 锥限隐藏侧向建筑。
- **跟踪感知门**(前向仿真预测真飞路径,门检查它):**10→2**,头对头 **0 输**。证实:point-mass 下全 0 碰撞 → 差距 100% 是跟踪过冲。

### 12. 树碰撞体积完善
- 渲染模式 getTightBounds 证实 WLH 略漏算(0.9 vs 真实 1.17)。
- 静态从"长方盒"改 **max(W,L) 圆柱**(metric + 体素一致,匹配圆树冠)。圆柱 30/30 到达,优于盒/方形。

### 13. 坠机=不到达 的 realistic 语义
- **碰撞→停止该集→不算到达**(双方一视同仁)。揭出 native 的"100 到达"含 29 次带撞冲过 → realistic 下 ours **安全完成 92 vs native 71**,反超 21。

### 14. 深度研究:HCT-D(16-agent workflow)
- 文献扫(FaSTrack/CCM/conformal/learning/跨机型)+ 5 候选设计 + 对抗评审 + 综合。
- 推荐 **HCT-D**(Hodograph-Conformal Tube):管子形状从 hodograph(加速度/jerk/曲率,**不是速度**)+ 常数 split-conformal κ + 加速度包络钳制(control 当前置条件不当保证)。
- 完整综合存 `docs/tracking-tube-research-2026-06.md`。
- **Stage 0-2 管线已建**(`track_harvest` 钩子 + `track_conformal`);数据证实:`corr(δ,速度)=−0.15`(速度是错特征),重尾=高加速度饱和。

---

## 关键产物(可复用)
- 评测:`render_screen.py`(100 场景)/ `planner_safety_matrix.py` / `compare3.py` / `ab_replay.py`
- 一致性:`render_3d_video.py --headless`(render=headless 同代码)
- 标定:`conformal_{harvest,calibrate}` / `track_{harvest,conformal}`
- 共享层:`safety_layer.py`、`cert_bridge.py`、`cert_capi`、GCOPTER vendored
- 数据/图:`out/conformal/render_screen.csv`、`fig_safe_completion.png`、`ab_s54.mp4`
- 文档:`docs/tracking-tube-research-2026-06.md`、`docs/progress-2026-06-24-25.md`(本文)

## 诚实的待办
1. **HCT-D conformal-残差那轮**:把"前向仿真门(working 但依赖模型)"升级成 model-free 分布无关保证(systematic)。
2. 真硬件标定(现在是 sim guarantee)。
3. 2 个"卡障碍内"硬场景的 stuck-recovery(纯到达,收益小)。
